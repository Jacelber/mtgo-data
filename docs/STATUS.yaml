schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/ci-pr-body-preflight-20260821-01"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-21"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "CI-PR-BODY-PREFLIGHT-01"
  name: "Local PR contract preflight and actionable admission failure"
  status: "owner_accepted_completion_authorized"
  implementation_authorized_on: "2026-08-21"
  implementation_completed_on: "2026-08-21"
  owner_accepted_on: "2026-08-21"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/ci-pr-body-preflight-20260821-01"
  branch: "codex/ci-pr-body-preflight-20260821-01"
  base_commit: "8c7d88d8ffba37853439b5d268ed6cc9fcca0f74"
  objective: "Prevent repeated remote CI failures caused by omitted or stale machine-readable PR-body evidence by validating the exact body against the exact local base-to-head diff before the first remote write, while preserving fail-closed CI admission."
  authorization:
    local_implementation: false
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - "ci_master_admission.py"
    - "tools/github_publication_preflight.ps1"
    - ".github/pull_request_template.md"
    - ".github/workflows/ci.yml"
    - "tests/test_ci_master_admission.py"
    - "tests/test_github_publication_preflight.py"
    - "tests/test_ci_workflow.py"
    - "AGENTS.md"
    - "docs/DEVELOPMENT_WORKFLOW.md"
    - "docs/ROADMAP.md"
    - "docs/STATUS.yaml"
  protected_scope:
    statistical_formulas_changed: "none"
    public_json_or_schema_changed: "none"
    generated_or_source_data_changed: "none"
    classification_identities_changed: "none"
    workflows_or_public_paths_changed: "change only PR admission feedback and the required pre-publication contract check; no production or Pages workflow change"
    event_434455_bytes_changed: false
    production_dispatched: false
  allowed_actions:
    - "Reuse the existing fail-closed admission classifier to validate one prepared PR body against the exact local base-to-head diff."
    - "Require that validation in the repository publication preflight before any GitHub authentication or permission check."
    - "Make the PR template invalid until its artifact-impact placeholder is deliberately replaced."
    - "Surface the exact admission reason in the aggregate CI failure."
    - "Add focused governance tests and update the authoritative publication instructions."
  prohibited_changes:
    - "Do not alter validation categories, validation strength, product code, statistical semantics, generated data, public paths, classifier rules, production timing, or Pages publication."
    - "Do not dispatch production or begin another task."
  stop_point: "Complete the accepted unchanged task through commit, one Ready PR, required CI, merge, and any applicable publication confirmation; then stop before another task."
  validation:
    evidence:
      - "70 focused admission, publication-preflight, and CI-workflow tests passed."
      - "Ruff passed for the changed Python admission and focused governance test files."
      - "Repository validation passed: Python 80/80, JavaScript 18/18, JSON 1823/1823, YAML 60/60, references 54/54, hygiene 2256/2256."
      - "Complete diff review and git diff --check passed with no product, data, statistical, classifier, production, or public-path change."

current_repository_state:
  baseline_commit: "8c7d88d8ffba37853439b5d268ed6cc9fcca0f74"
  baseline_note: "Cloud master after P12-10 merged and its applicable Pages publication completed."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - task: "CI-PR-BODY-PREFLIGHT-01"
    status: "owner_accepted_completion_authorized"
    remaining_requirements:
      - "Complete commit, one Ready PR, required CI, merge, and any applicable publication confirmation."
  - task: "P12-11"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Complete and close the current governance repair first."
      - "Obtain a separate task contract and implementation authorization."
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."

next_approved_task:
  id: "CI-PR-BODY-PREFLIGHT-01"
  name: "Local PR contract preflight and actionable admission failure"
  status: "owner_accepted_completion_authorized"
  local_execution_authorized: true
  note: "Owner accepted the unchanged repair on 2026-08-21. Complete commit, one Ready PR, required CI, merge, and any applicable publication confirmation without another authorization prompt, then stop."

prohibited_next_actions:
  - "Do not dispatch production."
  - "Do not begin P12-11, P12-12, Landing UI, default-entry cutover, or another task."
  - "Do not change product code, statistical semantics, generated data, public paths, classifier rules, validation strength, workflow timing, or event 434455 bytes."
  - "Do not force-push, mutate protected branches or tags directly, or create a second status-only pull request."
  - "Do not develop directly on master or overwrite unexplained local changes."
