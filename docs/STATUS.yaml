schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/classifier-restatement-02"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-18"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "CLASSIFIER-RESTATEMENT-01"
  name: "Current-classifier restatement with immutable source facts"
  status: "owner_accepted_completion_authorized"
  implementation_authorized_on: "2026-08-17"
  developer_validation_completed_on: "2026-08-17"
  owner_accepted_on: "2026-08-17"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/classifier-restatement-rebased-20260818-01"
  branch: "codex/classifier-restatement-02"
  base_commit: "0965702d71ef8d88dcbabce561e89741dfec3a8b"
  objective: "Make retained source facts authoritative, rebuild all retained Top 8 classifications and bases under the current classifier, and replace historical equality with deterministic digest and impact contracts."
  artifact_impact: "statistical_json_structure,generated_public_data,internal_diagnostics,governance"
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - "src/mtgmeta/classifier.py"
    - "src/mtgmeta/mtgo/stats.py"
    - "src/mtgmeta/mtgo/top8.py"
    - "src/mtgmeta/mtgo/__main__.py"
    - "tests/test_mtgo_top8_restatement.py"
    - "tests/test_generated_consumer_contracts.py"
    - "schemas/mtgo-top8-*.schema.json"
    - "schemas/manifest.json"
    - "stats/{standard,modern}/mtgo/top8/*.json"
    - ".github/workflows/ci.yml"
    - "docs/STATISTICS_SPEC.md"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/ROADMAP.md"
    - "docs/DECISIONS.md"
    - "docs/STATUS.yaml"
    - "docs/TEST_TRIGGER_MATRIX.md"
  protected_scope:
    statistical_formulas_changed: "none"
    public_json_or_schema_changed: "Top 8 additive classifier digest and Schema version 1.1.0"
    generated_or_source_data_changed: "derived Top 8 history only; source data unchanged"
    classification_identities_changed: "derived outputs restated under unchanged current rules"
    workflows_or_public_paths_changed: "focused PR validation only; public paths unchanged"
    event_434455_bytes_changed: false
    production_dispatched: false
  allowed_actions:
    - "Separate immutable event and placement source facts from derived classification and comparison values."
    - "Rebuild every indexed Top 8 week and comparison base with the current classifier."
    - "Require one common classifier digest and deterministic same-subject output."
    - "Treat Unknown as explicit derived output while keeping conflict and invalid-deck failures closed."
    - "Generate a de-identified classification impact report that is informative rather than a zero-difference gate."
    - "Update the applicable specifications, architecture, decision, Schemas, focused tests, and CI trigger matrix."
  prohibited_changes:
    - "Do not change classifier rules, taxonomy, formulas, raw events, match input, event 434455 source bytes, UI source, or public paths."
    - "Do not fetch data, dispatch production, close Issue 228, start P12-10, or begin another task."
  stop_point: "Complete the accepted unchanged task through one local commit, one Ready pull request, required CI, normal merge, and merge-triggered Pages verification; then stop without production dispatch, Issue closure, or another task."

current_repository_state:
  baseline_commit: "0965702d71ef8d88dcbabce561e89741dfec3a8b"
  baseline_note: "Verified remote master after GITHUB-PREFLIGHT-01 merged in PR 229; the accepted classifier implementation and generated outputs were replayed unchanged on that governance base."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."
  - task: "P12-10"
    status: "blocked_pending_no_publication_rehearsal_and_separate_authorization"
    remaining_requirements:
      - "Perform and accept one separately authorized no-publication Tuesday rehearsal."
      - "Obtain separate P12-10 implementation authorization."

next_approved_task:
  id: "NONE"
  name: "No follow-up task is authorized"
  status: "no_follow_up_authorized"
  local_execution_authorized: false
  note: "Owner acceptance authorizes continuous completion of the unchanged CLASSIFIER-RESTATEMENT-01 task through commit, one Ready pull request, required CI, normal merge, and merge-triggered Pages verification; no follow-up task is authorized."

prohibited_next_actions:
  - "Do not dispatch production, fetch protected source data, close Issue 228, or begin a follow-up task under the accepted-task completion authority."
  - "Do not begin the Melee v3 production preflight, P12-10, Pickup reflow, or another task automatically."
  - "Do not retain a second standalone P12-13A search state; P12-13B replaces row search and label focus with one exact multi-select row filter whose internal search only narrows filter candidates."
  - "Do not restore the superseded Top-N, P12-13D minimum-match filter, P12-13E URL-persisted matrix state, P12-13Z dedicated combination review, or the former four-direction focus-grid plan."
  - "Do not begin or redesign P12-14 or any later task until its own pre-implementation review."
  - "Do not change classifier rules, taxonomy, formulas, raw source data, UI source, or public paths."
  - "Do not create, access, configure, or rotate an HMAC key or GitHub Secret."
  - "Do not dispatch production, change event selection, fetch protected source data, or expand beyond the approved classifier-restatement contract."
  - "Do not rewrite Git history, force-push, mutate protected branches or tags, or change event 434455 raw snapshot or normalized-event bytes."
  - "Do not develop directly on master or overwrite unexplained local changes."
