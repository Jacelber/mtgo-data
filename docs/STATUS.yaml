schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/gov-10b-pages-recovery-subject"
  working_branch_status: "owner_accepted_same_task_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-16"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "GOV-10B-PAGES-RECOVERY-SUBJECT-BINDING"
  name: "Bind production recovery to the immutable Pages subject"
  status: "owner_accepted_same_task_completion_authorized"
  authorized_on: "2026-08-16"
  owner_accepted_on: "2026-08-16"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/gov-10a-pages-content-evidence-20260816-01"
  branch: "codex/gov-10b-pages-recovery-subject"
  base_commit: "b6f572f9da6e78dfe4a6d956b61cd95c6dd4b93a"
  artifact_impact: "public_path"
  objective: "Run recovery admission and packaging from the immutable master dispatch subject while accepting only a proven ancestor production commit whose generated output still matches that subject."
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - ".github/workflows/pages.yml"
    - "ci_master_admission.py"
    - "tests/test_ci_master_admission.py"
    - "tests/test_ci_workflow.py"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/DEVELOPMENT_WORKFLOW.md"
    - "docs/TEST_TRIGGER_MATRIX.md"
    - "docs/STATUS.yaml"
    - "docs/GOVERNANCE_REMEDIATION.yaml"
  planned_file_operations:
    additions: []
    deletions: []
    renames: []
  protected_scope:
    product_code_changed: "none"
    raw_or_generated_data_changed: "none"
    statistical_behavior_changed: "none"
    schema_changed: "none"
    frontend_or_ui_changed: "none"
    workflow_changed: "Only production-evidence dispatch admission and the immutable Pages checkout subject."
    public_paths_changed: "No URL changes; the Pages publication boundary changes."
    production_dispatched: false
    remote_write_performed: "none"
  validation_plan:
    - "Run only the changed production-evidence nodes, the one Pages workflow-contract node, and the live-document contract once on the final local tree."
    - "Do not run CLI, generator, candidate, Schema, rule, Node, Playwright, full pytest, or production checks locally."
    - "After Owner acceptance, targeted PR CI owns one independent control-plane validation; one recovery dispatch reuses run 31906796791 without regenerating or retesting its candidate."
  prohibited_changes:
    - "Do not weaken the immutable artifact SHA, generation-subject SHA, production-run provenance, ancestor requirement, output boundary, or failure-closed behavior."
    - "Do not add a test file, restore retired suites, regenerate data, modify public paths, or change statistics."
    - "Do not dispatch production or recovery Pages before Owner acceptance."
  stop_point: "Complete the accepted task continuously through commit, one Ready PR, required CI, merge, and one exact-evidence recovery dispatch; stop on failure or before any later task."

current_repository_state:
  baseline_commit: "b6f572f9da6e78dfe4a6d956b61cd95c6dd4b93a"
  baseline_note: "Remote master after GOV-10A merged through PR 220; recovery run 31923585217 exposed the stale-verifier and current-master-equality defects addressed by GOV-10B."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers:
  - id: "MTGO-PAGES-RUN-31923585217-STALE-SUBJECT-VERIFIER"
    scope: "governance_closeout_and_production_pages"
    effect: "Recovery checks out the historical publication commit before executing admission, so it runs the old verifier and rejects a valid ancestor publication after master advances."

future_task_gates:
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."
  - task: "P12-10"
    status: "blocked_pending_no_publication_rehearsal_and_separate_authorization"
    remaining_requirements:
      - "Resolve the GOV-10B Pages recovery-subject blocker."
      - "Perform and accept one separately authorized no-publication Tuesday rehearsal."
      - "Obtain separate P12-10 implementation authorization."

next_approved_task:
  id: "GOV-10B-PAGES-RECOVERY-SUBJECT-BINDING"
  name: "Bind production recovery to the immutable Pages subject"
  status: "owner_accepted_same_task_completion_authorized"
  local_execution_authorized: true
  note: "GOV-10B same-task completion is authorized; every later task remains unauthorized."

prohibited_next_actions:
  - "Do not begin the Melee v3 production preflight, P12-10, Pickup reflow, or another task automatically."
  - "Do not create, access, configure, or rotate an HMAC key or GitHub Secret."
  - "Do not dispatch production, change event selection, fetch protected source data, or change public/statistical contracts."
  - "Do not rewrite Git history, force-push, mutate protected branches or tags, or change event 434455 protected bytes."
  - "Do not develop directly on master or overwrite unexplained local changes."
