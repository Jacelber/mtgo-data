schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/gov-12-operations-privacy-closeout"
  working_branch_status: "owner_accepted_same_task_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-16"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "GOV-12-OPERATIONS-AND-PRIVACY-CLOSEOUT"
  name: "Document Melee HMAC recovery and production failure response"
  status: "owner_accepted_same_task_completion_authorized"
  authorized_on: "2026-08-16"
  owner_accepted_on: "2026-08-16"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/gov-12-operations-privacy-closeout-20260816-01"
  branch: "codex/gov-12-operations-privacy-closeout"
  base_commit: "de6ca64a4d2f34b0f24f983bf11e881bf3a7181a"
  artifact_impact: "none"
  objective: "Close the current documentation gaps for Melee-only HMAC recovery and existing production failure response without changing executable behavior or provisioning a secret."
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - "README.md"
    - "docs/STATUS.yaml"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/GOVERNANCE_REMEDIATION.yaml"
    - "docs/OPERATIONS_RUNBOOK.md"
  planned_file_operations:
    additions:
      - path: "docs/OPERATIONS_RUNBOOK.md"
        category: "docs"
    deletions: []
    renames: []
  protected_scope:
    product_code_changed: "none"
    raw_or_generated_data_changed: "none"
    statistical_behavior_changed: "none"
    schema_changed: "none"
    frontend_or_ui_changed: "none"
    workflow_changed: "none"
    public_paths_changed: "none"
    production_dispatched: false
    remote_write_performed: "none"
  validation_plan:
    - "Owner review and complete diff review are the local acceptance checks for this documentation-only task."
    - "Run no local automated test because executable behavior is unchanged."
    - "The final targeted PR CI owns one repository-reference validation and one existing live-document contract check; do not repeat either locally."
    - "Do not run privacy, CLI, Schema, rule, full pytest, Ruff, mypy, Node, Playwright, generator, live-fetch, production, or Pages checks."
  prohibited_changes:
    - "Do not change code, tests, Schemas, configuration, data, UI, workflows, public URLs, event selection, statistics, or production behavior."
    - "Do not create, read, provision, rotate, or expose a real HMAC key, credential, GitHub Secret, or private removal-request value."
    - "Do not add a suppression list or scan data, stats, reports, or Pages artifacts for participant strings."
    - "Do not delete or rename files; add only the predeclared docs/OPERATIONS_RUNBOOK.md."
    - "Do not dispatch production, deploy, provision a secret, or begin the Melee v3 preflight as part of accepted-task completion."
  stop_point: "Complete the unchanged accepted task through local commit, one Ready PR, required CI, and merge; stop on failed checks, changed scope, conflict, or permission blocker."

current_repository_state:
  baseline_commit: "de6ca64a4d2f34b0f24f983bf11e881bf3a7181a"
  baseline_note: "Remote master after GOV-11 PR 218; GitHub and Git retain its publication evidence."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers:
  - id: "MTGO-PRODUCTION-RUN-31838657545-CANCELLED"
    scope: "production_and_p12_10_rehearsal"
    effect: "A later scheduled production run must succeed before the separately authorized no-publication P12-10 rehearsal."

future_task_gates:
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."
  - task: "P12-10"
    status: "blocked_pending_no_publication_rehearsal_and_separate_authorization"
    remaining_requirements:
      - "Perform and accept one separately authorized no-publication Tuesday rehearsal."
      - "Obtain separate P12-10 implementation authorization."

next_approved_task:
  id: "NONE"
  name: "No follow-up task is authorized"
  status: "no_follow_up_authorized"
  local_execution_authorized: false
  note: "GOV-12 is Owner-accepted for same-task completion; the Melee v3 production preflight and every other follow-up remain unauthorized."

prohibited_next_actions:
  - "Do not begin the Melee v3 production preflight, GOV-13, P12-10, or product development without separate Owner authorization."
  - "Do not create, access, configure, or rotate an HMAC key or GitHub Secret."
  - "Do not dispatch production, change event selection, fetch protected source data, or change public/statistical contracts."
  - "Do not rewrite Git history, force-push, mutate protected branches or tags, or change event 434455 protected bytes."
  - "Do not develop directly on master or overwrite unexplained local changes."
