schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/melee-review-branch-path-shadowing-v1"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-31"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 14
  name: "Pauper MTGO and Paupergeddon"
  status: "active_melee_review_branch_path_shadowing_repair"
  started_on: "2026-08-30"
  objective: "Extend the existing shared multi-format product to Pauper without copying Standard or Modern implementations, keep MTGO and Tabletop products separate, and admit no partial public format."
  production_changes_allowed: true
  implementation_authorized: true
  next_phase_id: 15
  next_phase_authorized: false

current_task:
  id: "MELEE-REVIEW-BRANCH-PATH-SHADOWING-V1"
  name: "Prevent the Melee review-branch guard from shadowing the shell command path"
  status: "owner_accepted_completion_authorized"
  authorized_on: "2026-08-31"
  base_commit: "365bf91e1739aa646b94a43d5455f46525c7dcc9"
  branch: "codex/melee-review-branch-path-shadowing-v1"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/melee-review-branch-path-shadowing-v1-20260831-01"
  objective: "Rename the review-branch changed-file loop variable that overwrites Bash PATH, preserve the existing path allowlist, and add a focused contract regression before any event 441441 retry."
  artifact_impact: "none"
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  allowed_paths:
    - ".github/workflows/fetch_melee.yml"
    - "tests/test_ci_workflow.py"
    - "docs/STATUS.yaml"
  planned_additions: []
  protected_scope:
    source_or_whitelist_change: "none"
    data_or_candidate_change: "none"
    collection_or_recovery_dispatch: "prohibited"
    workflow_behavior_change: "only rename the local changed-file variable; keep the existing review-branch allowlist, failure conditions, permissions, triggers, and push boundary"
    schema_statistics_taxonomy_or_public_path_change: "none"
  invariants:
    - "The resume step must inspect the same git diff and accept or reject exactly the same changed paths as before."
    - "No loop variable may overwrite Bash PATH before the resume step finishes using git."
    - "The workflow remains manual-only and may push only the bounded event review branch."
    - "No event workflow is dispatched and no retained or generated data is touched during this repair."
  prohibited_changes:
    - "Do not change workflow triggers, permissions, schedules, event path allowlists, push targets, source selection, checkpoint behavior, or candidate semantics."
    - "Do not change configs, source or generated data, Schemas, taxonomy, statistics, front ends, or public paths."
    - "Do not dispatch or retry event 441441, recollect an event, create a candidate pull request, or deploy Pages."
  planned_verification:
    - "First make the focused Melee checkpoint workflow-contract node reject the current PATH-shadowing implementation."
    - "After the repair, rerun only that exact node plus the live STATUS contract node."
    - "Run changed-scope repository validation from base 365bf91e, git diff --check, and complete changed-path and diff review."
    - "Do not run collection, candidate generation, full pytest, browser, MTGO, Pages, or deployment checks."
  stop_point: "Complete the unchanged accepted repair through one Ready PR and merge, then stop; any event 441441 retry remains separately unauthorized."
  owner_acceptance:
    status: "accepted"
    accepted_on: "2026-08-31"
    subject: "The exact three-path local repair for MELEE-REVIEW-BRANCH-PATH-SHADOWING-V1."

current_repository_state:
  source_master_commit: "365bf91e1739aa646b94a43d5455f46525c7dcc9"
  previous_task: "MELEE-441441-CANDIDATE-RECOVERY"
  previous_task_status: "activation_merged_recovery_run_failed_before_source_access"
  activation_pull_request: 329
  activation_merge_commit: "365bf91e1739aa646b94a43d5455f46525c7dcc9"
  failed_melee_candidate_run: 33399754510
  failure_stage: "Resume retained event review branch when present"
  retained_candidate_branch: "data/melee-441441"
  retained_checkpoint_commit: "63dd814eeb31aeabc58f20397e8441056df18052"
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - id: "MELEE-REVIEW-BRANCH-PATH-SHADOWING-V1"
    status: "owner_accepted_completion_authorized"
    note: "Complete the unchanged accepted repair through one Ready PR and merge, then stop."
  - id: "MELEE-441441-CANDIDATE-RECOVERY-RETRY"
    status: "not_authorized"
    note: "A retry is a separate event operation and begins only after this workflow repair is accepted, published, merged, and explicitly authorized."
  - id: "MELEE-441441-CANDIDATE-ACCEPTANCE"
    status: "pending_successful_candidate_evidence"
    note: "Owner data, classification, statistics, and quality review begins only after a successful recovered candidate exists."
  - id: "MELEE-441441-PUBLICATION"
    status: "not_authorized"
    note: "Candidate PR creation, merge, Pages deployment, and live publication require later Owner acceptance of the exact candidate."
  - id: "P14-02"
    status: "planned_not_authorized"
    note: "Pauper product input does not begin implicitly after this repair."

next_approved_task:
  id: "MELEE-REVIEW-BRANCH-PATH-SHADOWING-V1"
  name: "Complete the accepted PATH-shadowing repair"
  status: "owner_accepted_completion_authorized"
  local_execution_authorized: true
  commit_authorized: true
  publication_authorized: true
  merge_authorized: true
  note: "Continuous completion applies only to the unchanged accepted three-path repair; event retry remains separately gated."

prohibited_next_actions:
  - "Do not dispatch or retry event 441441 or mutate its remote review branch during this repair completion."
  - "Do not recollect event 441441, change its whitelist or retained checkpoint, publish candidate data, deploy Pages, begin P14-02, Phase 15, another event admission, or unrelated repository work."
