schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/weekly-landing-machine-fact-binding-w34-20260825"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-25"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the MTGO and Tabletop front ends and complete the approved Landing migration without changing statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "WEEKLY-LANDING-MACHINE-FACT-BINDING-2026-W34"
  batch_id: "WEEKLY-LANDING-MACHINE-FACT-BINDING-2026-W34"
  name: "Repair the Landing import-to-preview machine-fact binding contract"
  status: "owner_accepted_completion_authorized"
  implementation_authorized_on: "2026-08-25"
  owner_accepted_on: "2026-08-25"
  developer_validation_completed_on: "2026-08-25"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/weekly-landing-machine-fact-binding-w34-20260825-01"
  branch: "codex/weekly-landing-machine-fact-binding-w34-20260825"
  base_commit: "1f6267f8c6a7f4025f1397e98ee72bb2806162e3"
  task_type: "bounded_private_landing_binding_contract_repair"
  artifact_impact: "internal_diagnostics"
  objective: "Use one exact Landing machine-fact digest across readiness, workbook import, and preview admission so an unchanged accepted W34 workbook can produce a local preview while real fact drift still fails closed."
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  production_follow_up:
    authorized: false
    condition: "Do not dispatch MTGO production during or after this task."
  target_paths:
    - "src/mtgmeta/mtgo/landing.py"
    - "src/mtgmeta/mtgo/landing_editorial.py"
    - "tests/test_mtgo_landing.py"
    - "tests/test_mtgo_landing_editorial.py"
    - "docs/WEEKLY_MAINTENANCE.md"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/DECISIONS.md"
    - "docs/STATUS.yaml"
  planned_file_operations:
    additions: []
    deletions: []
    renames: []
  protected_scope:
    public_json_or_schema_changed: "none"
    generated_or_source_data_changed: "none"
    classification_identities_changed: "none"
    visual_identity_changed: "none"
    workflows_or_public_paths_changed: "none"
    event_434455_bytes_changed: false
    production_dispatched: false
  allowed_actions:
    - "Add one exact-week Landing machine-fact digest accessor that excludes human review admission."
    - "Make the shared Top 8 subject use that digest and reject a different non-null candidate digest as stale."
    - "Add focused regression coverage and document the single-source machine contract."
    - "Validate the accepted W34 workbook through temporary Standard and Modern import-to-preview outputs."
  prohibited_changes:
    - "Do not change the accepted W34 workbook or import it into repository review state."
    - "Do not change classifier rules, bilingual identities, statistics, source data, generated public data, Landing UI, public paths, or workflow scheduling."
    - "Do not import repository review state before this repair merges; after merge resume only the authorized W34 local import and preview."
    - "Do not deploy or dispatch MTGO production."
  validation:
    regression: "pass; the Top 8 subject uses the authoritative exact-week Landing machine-fact digest, that digest equals the Landing review binding for W34, and changed machine-fact bindings still fail closed"
    w34_round_trip: "pass; exact workbook b4ba2e64... imported in a temporary root and generated Standard W34 with 4 summary and 5 feature items plus Modern W34 with 4 summary and 3 feature items"
    focused_suite: "pass; 34 related Landing, editorial, and live-documentation tests pass; one W33 current-review assertion was deselected after the same failure was reproduced on the untouched 1f6267 baseline"
    lint: "pass; Ruff accepts all four changed Python and test files"
    documentation: "pass; the focused live-status contract is included in the 34 passing tests"
    diff_check: "pass; exactly the eight declared files are modified, no workbook, private review state, generated public data, source data, or workflow file changed, and git diff --check is clean"
  stop_point: "Commit the unchanged accepted repair, publish one Ready pull request, wait for required CI, and merge normally; after merge resume the authorized W34 repository import and local preview without dispatching MTGO production."

current_repository_state:
  baseline_commit: "1f6267f8c6a7f4025f1397e98ee72bb2806162e3"
  baseline_note: "Remote master remains the exact verified task base; Owner acceptance authorizes commit, one Ready pull request, required CI, and normal merge, followed only by the authorized W34 local import and preview without production."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - task: "WEEKLY-LANDING-CONTENT-2026-W34"
    status: "import_preview_authorized_after_machine_fact_repair_merge"
    note: "After this accepted repair merges, resume the accepted W34 workbook repository import and local preview; public Landing publication and MTGO production remain separately gated."
  - task: "P12-16"
    status: "planned_not_authorized"
    note: "Cross-device and visual-system closeout remains a separate task after the W34 maintenance gate."

next_approved_task:
  id: "WEEKLY-LANDING-MACHINE-FACT-BINDING-2026-W34"
  name: "Complete and publish the accepted Landing machine-fact binding repair"
  status: "owner_accepted_completion_authorized"
  local_execution_authorized: true
  note: "Commit, publish one Ready PR, wait for required CI, and merge the unchanged accepted subject; after merge resume only the authorized W34 repository import and local preview."

prohibited_next_actions:
  - "Do not import W34 repository review state before this repair merges."
  - "Do not deploy or dispatch MTGO production."
  - "Do not change classifier conditions, bilingual identities, statistics, generated public outputs, visual identities, UI behavior, source data, public paths, workflow scheduling, or protected event 434455 bytes."
  - "Do not develop directly on master or overwrite unexplained local changes."
