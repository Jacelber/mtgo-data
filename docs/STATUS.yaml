schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/weekly-maintenance-workflow-01"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-19"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "WEEKLY-MAINTENANCE-WORKFLOW-01"
  name: "Cloud-triggered weekly MTGO maintenance handoff"
  status: "owner_accepted_completion_authorized"
  implementation_authorized_on: "2026-08-19"
  developer_validation_completed_on: "2026-08-19"
  owner_accepted_on: "2026-08-19"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/weekly-maintenance-workflow-independent-20260819-01"
  branch: "codex/weekly-maintenance-workflow-01"
  base_commit: "120c4431457870657eccd04754a8724b195757e3"
  objective: "Move daily MTGO production to 18:00 JST and create a private, deduplicated cloud handoff for the Owner-started weekly maintenance review without depending on Codex scheduling."
  artifact_impact: "internal_diagnostics"
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - ".github/workflows/update.yml"
    - "tools/generate_weekly_maintenance_readiness.py"
    - "schemas/weekly-maintenance-readiness.schema.json"
    - "tests/test_weekly_maintenance_readiness.py"
    - "tests/test_ci_workflow.py"
    - "docs/WEEKLY_MAINTENANCE.md"
    - "docs/PROJECT_SCOPE.md"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/ROADMAP.md"
    - "docs/DECISIONS.md"
    - "docs/STATUS.yaml"
    - "docs/TEST_TRIGGER_MATRIX.md"
  protected_scope:
    statistical_formulas_changed: "none"
    public_json_or_schema_changed: "none; the new Schema validates a private Actions artifact only"
    generated_or_source_data_changed: "none"
    classification_identities_changed: "none"
    workflows_or_public_paths_changed: "daily schedule moves to 09:00 UTC; one private readiness artifact and deduplicated Issue are added; public paths unchanged"
    event_434455_bytes_changed: false
    production_dispatched: false
  allowed_actions:
    - "Change the existing daily MTGO production cron from 20:00 UTC to 09:00 UTC."
    - "Generate one private weekly readiness artifact after a successful production outcome."
    - "Create or update one deduplicated weekly readiness Issue with no checkout or repository write permission."
    - "Record current-week Unknown review inputs, classification blockers, Pickup candidate counts, and explicit unavailable manual inputs."
    - "Document the Owner-started weekly sequence and human-final editorial authority."
    - "Run only the focused generator, Schema, workflow, and live-document checks required by the changed paths."
  prohibited_changes:
    - "Do not change classifier rules, taxonomy, visual metadata, Pickup approval or known state, statistics, generated production data, source data, UI, or public paths."
    - "Do not implement the P12-10 Landing producer, generate Landing content, or publish a Landing."
    - "Do not dispatch production or create the live weekly Issue during local development."
    - "Do not commit, push, open a pull request, merge, deploy, or begin the rehearsal without later Owner authorization."
  stop_point: "Complete the accepted unchanged task through one local commit, one Ready pull request, required CI, normal merge, and post-merge verification; then stop without production dispatch, weekly rehearsal, or P12-10."

current_repository_state:
  baseline_commit: "120c4431457870657eccd04754a8724b195757e3"
  baseline_note: "Verified remote master after the classifier-restatement merge and subsequent successful MTGO production publication. The prior live STATUS entry was stale and is superseded by the Owner's explicit 2026-08-19 task authorization."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - task: "WEEKLY-MAINTENANCE-REHEARSAL-01"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Accept and publish WEEKLY-MAINTENANCE-WORKFLOW-01 through its separate gates."
      - "Run one no-publication rehearsal against the current weekly cloud artifact under separate authorization."
      - "Record Owner dispositions for Unknown, visual metadata, and Weekly Pickup without starting Landing implementation."
  - task: "P12-10"
    status: "blocked_pending_weekly_maintenance_rehearsal_and_separate_authorization"
    remaining_requirements:
      - "Complete and accept one no-publication weekly maintenance rehearsal."
      - "Confirm the classifier, known-state, threshold, and representative-card prerequisites recorded in the authoritative Phase 12 contracts."
      - "Obtain separate P12-10 implementation authorization."
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."

next_approved_task:
  id: "NONE"
  name: "No follow-up task is authorized"
  status: "no_follow_up_authorized"
  local_execution_authorized: false
  note: "Owner acceptance authorizes continuous completion of the unchanged WEEKLY-MAINTENANCE-WORKFLOW-01 task through commit, one Ready pull request, required CI, normal merge, and post-merge verification; no follow-up task is authorized."

prohibited_next_actions:
  - "Do not dispatch production, create a live weekly Issue, fetch protected source data, or begin the weekly rehearsal automatically."
  - "Do not begin P12-10, generate Landing content, or treat the readiness artifact as Landing output."
  - "Do not change classifier rules, taxonomy, visual metadata, Pickup approval, formulas, raw source data, UI source, public paths, or event 434455 bytes."
  - "Do not force-push, mutate protected branches or tags directly, dispatch production, or begin another task under this accepted-task completion authority."
  - "Do not develop directly on master or overwrite unexplained local changes."
