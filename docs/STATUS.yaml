schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/github-preflight-context-01"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-18"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  authoritative_for:
    - "current phase"
    - "current task and authorization"
    - "known blockers"
    - "next planned task"
    - "prohibited next actions"
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 12
  name: "Front-end productization, editorial landing, and visual system"
  status: "active"
  objective: "Productize the existing MTGO and Tabletop front ends and add the approved MTGO Landing without changing established statistical meaning or product separation."
  production_changes_allowed: false
  next_phase_id: 13
  next_phase_authorized: false

current_task:
  id: "GITHUB-PREFLIGHT-01"
  name: "Structured GitHub publication preflight"
  status: "owner_accepted_completion_authorized"
  implementation_authorized_on: "2026-08-17"
  developer_validation_completed_on: "2026-08-17"
  owner_accepted_on: "2026-08-18"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/github-preflight-20260817-01"
  branch: "codex/github-preflight-context-01"
  base_commit: "adc341f469aa83b26cc81ba6ecd50a230a06aa90"
  objective: "Replace ambiguous text-based GitHub authentication detection with structured active-account state, remove the redundant authenticated-user API dependency, and classify GitHub HTTP 5xx failures as NETWORK_ERROR without weakening publication authorization checks."
  artifact_impact: "internal_diagnostics,governance"
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  target_paths:
    - "tools/github_publication_preflight.ps1"
    - "tests/test_github_publication_preflight.py"
    - "ci_master_admission.py"
    - "tests/test_ci_master_admission.py"
    - ".github/workflows/ci.yml"
    - "docs/DEVELOPMENT_WORKFLOW.md"
    - "docs/STATUS.yaml"
    - "docs/TEST_TRIGGER_MATRIX.md"
  protected_scope:
    statistical_formulas_changed: "none"
    public_json_or_schema_changed: "none"
    generated_or_source_data_changed: "none"
    classification_identities_changed: "none"
    workflows_or_public_paths_changed: "PR validation routing only; public paths unchanged"
    event_434455_bytes_changed: false
    production_dispatched: false
  allowed_actions:
    - "Read the active GitHub account, authentication state, and scopes from gh auth status structured JSON without exposing a token."
    - "Verify repository push permission through the existing repository permissions API call."
    - "Classify HTTP 5xx and equivalent GitHub availability failures as NETWORK_ERROR."
    - "Add focused cross-platform tests and route the preflight script as governance validation."
    - "Run the final script once without actual-context authority and once read-only in the actual publication context."
  prohibited_changes:
    - "Do not display, read, copy, persist, provision, refresh, or rotate any credential or token."
    - "Do not weaken the required Jacelber identity, repository push permission, workflow scope, actual-context, or fail-closed checks."
    - "Do not edit product code, classifier code, data, generated output, Schemas, public paths, or production workflows."
    - "Do not change or rewrite the accepted CLASSIFIER-RESTATEMENT-01 commit."
    - "Do not dispatch production, close Issue 228, or begin an unrelated task under this completion authority."
  stop_point: "Complete the accepted unchanged GITHUB-PREFLIGHT-01 tree through one local commit, one Ready pull request, required CI, and normal merge; then resume the separately accepted CLASSIFIER-RESTATEMENT-01 publication without beginning another task."

current_repository_state:
  baseline_commit: "adc341f469aa83b26cc81ba6ecd50a230a06aa90"
  baseline_note: "Verified remote master used for the isolated GITHUB-PREFLIGHT-01 workspace; the accepted classifier-restatement commit remains separate and unchanged."
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - task: "MELEE-V3-PRODUCTION-PREFLIGHT"
    status: "planned_not_authorized"
    remaining_requirements:
      - "Select and approve one test event and decide whether its snapshot will be retained."
      - "Choose a test-only key or production-managed key accordingly, with a unique non-secret key ID."
      - "Obtain separate authorization for secret provisioning, workflow changes, live collection, and any remote candidate branch."
      - "Keep the rehearsal non-deploying and review the complete v3 candidate before any public use."
  - task: "P12-10"
    status: "blocked_pending_no_publication_rehearsal_and_separate_authorization"
    remaining_requirements:
      - "Perform and accept one separately authorized no-publication Tuesday rehearsal."
      - "Obtain separate P12-10 implementation authorization."

next_approved_task:
  id: "NONE"
  name: "No follow-up task is authorized"
  status: "no_follow_up_authorized"
  local_execution_authorized: false
  note: "The unchanged GITHUB-PREFLIGHT-01 repair is Owner-accepted for continuous completion, followed only by resumption of the separately accepted CLASSIFIER-RESTATEMENT-01 publication."

prohibited_next_actions:
  - "Do not bypass the repaired preflight while publishing either accepted task."
  - "Do not resume CLASSIFIER-RESTATEMENT-01 publication until the repaired preflight is merged."
  - "Do not begin the Melee v3 production preflight, P12-10, Pickup reflow, or another task automatically."
  - "Do not retain a second standalone P12-13A search state; P12-13B replaces row search and label focus with one exact multi-select row filter whose internal search only narrows filter candidates."
  - "Do not restore the superseded Top-N, P12-13D minimum-match filter, P12-13E URL-persisted matrix state, P12-13Z dedicated combination review, or the former four-direction focus-grid plan."
  - "Do not begin or redesign P12-14 or any later task until its own pre-implementation review."
  - "Do not change matchup formulas, public JSON, Schemas, workflows, public paths, generated data, or classification identities."
  - "Do not create, access, configure, or rotate an HMAC key or GitHub Secret."
  - "Do not dispatch production, change event selection, fetch protected source data, or expand beyond the approved P12-13F horizontal keyboard-scroll contract."
  - "Do not rewrite Git history, force-push, mutate protected branches or tags, or change event 434455 raw snapshot or normalized-event bytes."
  - "Do not develop directly on master or overwrite unexplained local changes."
