schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/melee-candidate-repository-validation-mode-v1"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-09-01"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 14
  name: "Pauper MTGO and Paupergeddon"
  status: "active_melee_candidate_repository_validation_mode_repair"
  started_on: "2026-08-30"
  objective: "Extend the existing shared multi-format product to Pauper without copying Standard or Modern implementations, keep MTGO and Tabletop products separate, and admit no partial public format."
  production_changes_allowed: true
  implementation_authorized: true
  next_phase_id: 15
  next_phase_authorized: false

current_task:
  id: "MELEE-CANDIDATE-REPOSITORY-VALIDATION-MODE-V1"
  name: "Give Melee candidate repository validation an explicit full mode"
  status: "owner_accepted_completion_authorized"
  authorized_on: "2026-08-31"
  base_commit: "39ecc8efaca816fcea83c10903c3a75f434a81a9"
  branch: "codex/melee-candidate-repository-validation-mode-v1"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/melee-candidate-repository-validation-mode-v1-20260831-01"
  objective: "Pass --full to the Melee production candidate repository validator and extend the focused workflow contract so every production repository validation selects an explicit mode."
  artifact_impact: "none"
  authorization:
    local_implementation: true
    commit: true
    remote_publication: true
    merge: true
  allowed_paths:
    - ".github/workflows/fetch_melee.yml"
    - "tests/test_ci_workflow.py"
    - "docs/STATUS.yaml"
  planned_additions: []
  protected_scope:
    validation_strength: "preserve complete repository validation; do not replace it with changed-scope validation or skip any downstream rule or Schema check"
    workflow_change: "only add the required --full mode to the existing Melee repository validation command"
    test_change: "extend the existing production-validation mode contract to cover both MTGO and Melee workflows"
    workflow_permissions_triggers_schedules_or_push_boundaries: "none"
    data_candidate_whitelist_schema_statistics_taxonomy_or_public_path_change: "none"
    collection_or_recovery_dispatch: "prohibited"
  invariants:
    - "The Melee candidate workflow must invoke validate_repository.py with exactly one explicit --full mode before candidate staging."
    - "The MTGO production workflow keeps the same explicit --full command."
    - "The focused contract fails if either production workflow returns to a bare validate_repository.py invocation."
    - "No event workflow is dispatched and no retained or generated event data is touched during this repair."
  prohibited_changes:
    - "Do not change workflow triggers, permissions, schedules, candidate path allowlists, source selection, checkpoint behavior, push targets, rule validation, or Schema validation."
    - "Do not change configs, source or generated data, Schemas, taxonomy, statistics, front ends, public paths, or validation semantics outside the missing mode argument."
    - "Do not dispatch or retry event 441441, recollect an event, create a candidate pull request, or deploy Pages."
  planned_verification:
    - "First extend and run only the production repository-validation mode node, proving the current bare Melee invocation fails the contract."
    - "After the repair, rerun that exact node plus the live STATUS contract node."
    - "Run changed-scope repository validation from base 39ecc8ef, git diff --check, and complete changed-path and diff review."
    - "Do not run collection, candidate generation, full pytest, browser, MTGO, Pages, or deployment checks."
  stop_point: "Complete the unchanged accepted repair through one Ready PR and merge, then stop; any new event 441441 retry remains separately unauthorized."
  owner_acceptance:
    status: "accepted"
    accepted_on: "2026-09-01"
    subject: "The exact three-path local repair for MELEE-CANDIDATE-REPOSITORY-VALIDATION-MODE-V1."

current_repository_state:
  source_master_commit: "39ecc8efaca816fcea83c10903c3a75f434a81a9"
  previous_task: "MELEE-441441-CANDIDATE-RECOVERY-RETRY"
  previous_task_status: "activation_merged_single_dispatch_failed_before_candidate_commit"
  previous_pull_request: 331
  previous_merge_commit: "39ecc8efaca816fcea83c10903c3a75f434a81a9"
  failed_melee_candidate_run: 33404525027
  failed_run_stage: "Validate repository, rules, and public Schemas"
  failed_run_cause: "validate_repository.py requires exactly one of --changed-from or --full, but fetch_melee.yml passed neither"
  failed_run_source_mode: "retained"
  retained_candidate_branch: "data/melee-441441"
  retained_checkpoint_commit: "daa4773850a7acb172e495cc6bbfa8421a45f1de"
  retained_source_bytes_changed: false
  candidate_commit_created: false
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers: []

future_task_gates:
  - id: "MELEE-CANDIDATE-REPOSITORY-VALIDATION-MODE-V1"
    status: "owner_accepted_completion_authorized"
    note: "Complete the unchanged accepted workflow and test repair through one Ready PR and merge, then stop."
  - id: "MELEE-441441-CANDIDATE-RECOVERY-RETRY-2"
    status: "not_authorized"
    note: "A new dispatch is a separate event operation after this repair is accepted, published, and merged."
  - id: "MELEE-441441-CANDIDATE-ACCEPTANCE"
    status: "pending_successful_candidate_evidence"
    note: "Owner data, classification, statistics, and quality review begins only after a successful persisted candidate exists."
  - id: "MELEE-441441-PUBLICATION"
    status: "not_authorized"
    note: "Candidate PR creation, merge, Pages deployment, and live publication require later Owner acceptance of the exact candidate."
  - id: "P14-02"
    status: "planned_not_authorized"
    note: "Pauper product input does not begin implicitly after this repair."

next_approved_task:
  id: "MELEE-CANDIDATE-REPOSITORY-VALIDATION-MODE-V1"
  name: "Complete the accepted Melee repository-validation mode repair"
  status: "owner_accepted_completion_authorized"
  local_execution_authorized: true
  commit_authorized: true
  publication_authorized: true
  merge_authorized: true
  note: "Continuous completion applies only to the unchanged accepted three-path repair; any event retry remains separately gated."

prohibited_next_actions:
  - "Do not dispatch or retry event 441441 or mutate its remote review branch during this repair completion."
  - "Do not recollect event 441441, change its whitelist or retained checkpoint, publish candidate data, deploy Pages, begin P14-02, Phase 15, another event admission, or unrelated repository work."
