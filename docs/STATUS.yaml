schema_version: "1.0"

project:
  name: "mtgo-data"
  repository: "https://github.com/Jacelber/mtgo-data"
  default_branch: "master"
  current_working_branch: "codex/melee-missing-decklist-coverage-v1"
  working_branch_status: "owner_accepted_completion_authorized"
  status: "active"

status_document:
  purpose: "Small machine-readable record of current state and authorization."
  last_updated: "2026-08-31"
  updated_by: "project-owner-with-ai-assistance"
  live_state_only: true
  history_policy: "Completed detail belongs in docs/history, audits, decisions, pull requests, and Git history; never use it to authorize current work."

current_phase:
  id: 14
  name: "Pauper MTGO and Paupergeddon"
  status: "active_melee_missing_decklist_contract_owner_accepted"
  started_on: "2026-08-30"
  objective: "Extend the existing shared multi-format product to Pauper without copying Standard or Modern implementations, keep MTGO and Tabletop products separate, and admit no partial public format."
  production_changes_allowed: false
  implementation_authorized: false
  next_phase_id: 15
  next_phase_authorized: false

current_task:
  id: "MELEE-MISSING-DECKLIST-COVERAGE-V1"
  name: "Represent unavailable Melee decklists without fabricating Unknown classifications"
  status: "owner_accepted_completion_authorized"
  authorized_on: "2026-08-31"
  base_commit: "0cbf0713ac529b501fd102a40ac2063a7f6cdd1a"
  branch: "codex/melee-missing-decklist-coverage-v1"
  workspace: "D:/dl/crawlerpj/.codex-workspaces/melee-missing-decklist-coverage-v1-20260831-01"
  objective: "Keep every normalized participant and trustworthy event-level match fact while separating missing or unavailable decklists from Unknown, excluding unavailable identities from deck-share and matchup denominators, checkpointing retained evidence before derived work, and rejecting undefined Owner-acceptance states before publication routing."
  artifact_impact: "statistical_json_structure"
  authorization:
    local_implementation: false
    commit: true
    remote_publication: true
    merge: true
  allowed_paths:
    - ".github/workflows/fetch_melee.yml"
    - "src/mtgmeta/melee/opportunities.py"
    - "src/mtgmeta/melee/stats.py"
    - "src/mtgmeta/melee/matchup.py"
    - "schemas/melee-opportunity-ledger.schema.json"
    - "schemas/melee-event-overview.schema.json"
    - "schemas/melee-event-decks.schema.json"
    - "schemas/melee-event-matchup.schema.json"
    - "schemas/melee-event-quality.schema.json"
    - "tests/test_melee_missing_decklist_contract.py"
    - "tests/test_ci_workflow.py"
    - "tests/test_documentation_history.py"
    - "docs/STATISTICS_SPEC.md"
    - "docs/DATA_ARCHITECTURE.md"
    - "docs/DECISIONS.md"
    - "docs/STATUS.yaml"
  planned_additions:
    - path: "tests/test_melee_missing_decklist_contract.py"
      category: "maintained Python regression test"
      purpose: "Lock participant, submitted-deck, Unknown, unavailable-decklist, event-match, and matchup conservation without using live source data."
  protected_scope:
    product_or_statistical_change: "Narrow Melee data-contract correction: public participant coverage remains complete, while deck-share and classification matchup denominators contain only submitted classifiable decklists."
    generated_or_source_data_change: "none; do not generate, edit, or retain event 441441 or any other event data in this task"
    source_scope_change: "none; the whitelist and eligible event IDs remain unchanged"
    workflow_change: "Checkpoint raw plus normalized Melee evidence immediately after retention and reuse that review-branch snapshot on a later candidate continuation."
    governance_change: "Restrict current_task.owner_acceptance.status to the canonical pending and accepted states and reject every undefined value."
    permission_change: "none; preserve contents: write and existing bounded event concurrency"
    issue_change: "none"
    production_dispatch_authorized_after_merge: false
    production_dispatched: false
    remote_write_performed: "none"
  invariants:
    - "A missing or unavailable decklist is not Unknown; Unknown remains a valid submitted deck that matched no maintained archetype rule."
    - "Every normalized participant remains represented in the opportunity ledger and per-deck document, including their trustworthy event-level points and result facts."
    - "Metagame shares use submitted classified-or-Unknown decks as their denominator; unavailable decklists are counted separately and never receive a fabricated archetype identity."
    - "A classification matchup requires identities for both competitors; otherwise the physical match is retained as a source fact and counted as excluded for decklist_unavailable."
    - "The workflow may reuse only an immutable raw and normalized snapshot already saved on the exact event review branch; it never pushes directly to master and never force-pushes."
    - "Owner acceptance uses only pending or accepted; accepted atomically requires commit, remote_publication, and merge authority for the unchanged same-task completion path."
  prohibited_changes:
    - "Do not change configs/melee_events.yaml, collect or recollect Melee source data, dispatch a workflow, or create generated/public event artifacts."
    - "Do not change classifiers, taxonomy rules, MTGO behavior, front ends, schedules, workflow permissions, or unrelated repository paths."
    - "Do not dispatch production, collect or recollect event 441441, deploy outside the merge-triggered publication path, or mutate unrelated remote resources."
  planned_verification:
    - "Run only the new synthetic missing-decklist contract nodes, directly affected fetch_melee workflow contract nodes, and live-status canonical acceptance-state nodes with an external pytest base directory."
    - "Run ruff and mypy only against the three changed maintained Melee Python modules, validate_schemas.py once, validate_repository.py --changed-from 0cbf0713ac529b501fd102a40ac2063a7f6cdd1a once on the final tree, review the complete diff, and run git diff --check."
    - "Do not run live fetch, candidate generation, browser, Pages, archetype-rule, full pytest, production, or deployment workflows."
  stop_point: "Complete the accepted unchanged expanded task through one commit, one Ready pull request, required CI, and merge; stop before event collection, production dispatch, or another task."
  owner_acceptance:
    status: "accepted"
    accepted_on: "2026-08-31"
    subject: "The expanded MELEE-MISSING-DECKLIST-COVERAGE-V1 implementation, including rejection of undefined Owner-acceptance states."

current_repository_state:
  source_master_commit: "0cbf0713ac529b501fd102a40ac2063a7f6cdd1a"
  previous_task: "GOV-MTGO-INCONSISTENT-EVENT-RESPONSE-RECOVERY"
  previous_task_status: "completed_merged_production_and_pages_verified"
  previous_pull_request: 324
  previous_merge_commit: "931f604"
  previous_merge_validation_run: 33380825643
  previous_production_commit: "0cbf0713ac529b501fd102a40ac2063a7f6cdd1a"
  previous_pages_run: 33383790213
  failed_melee_candidate_run: 33331402773
  public_entries:
    mtgo: "/index.html"
    tabletop: "/melee/index.html"
  public_catalog: "/stats/catalog.json"

known_blockers:
  - id: "MELEE-MISSING-DECKLIST-COVERAGE"
    status: "owner_accepted_completion_authorized"
    note: "The expanded immutable subject is Owner-accepted for completion through one Ready pull request, required CI, and merge."

future_task_gates:
  - id: "MELEE-MISSING-DECKLIST-COVERAGE-V1"
    status: "owner_accepted_completion_authorized"
    note: "Complete the accepted unchanged expanded subject through one Ready pull request, required CI, and merge without another authorization prompt."
  - id: "MELEE-441441-PRODUCTION-CANDIDATE"
    status: "blocked_pending_expanded_repair_completion"
    note: "Do not recollect or resume event 441441; complete the expanded repair first, while candidate resumption remains a later separately governed operation."
  - id: "P14-02"
    status: "planned_not_authorized"
    note: "Pauper product input does not begin implicitly after this repair."
  - id: "PHASE-15"
    status: "planned_not_authorized"
    note: "Pioneer does not begin implicitly after Phase 14."

next_approved_task:
  id: "MELEE-MISSING-DECKLIST-COVERAGE-V1"
  name: "Represent unavailable Melee decklists without fabricating Unknown classifications"
  status: "owner_accepted_completion_authorized"
  local_execution_authorized: false
  commit_authorized: true
  publication_authorized: true
  merge_authorized: true
  note: "Complete only the accepted unchanged expanded subject through one Ready pull request, required CI, and merge."

prohibited_next_actions:
  - "Do not dispatch production, collect or recollect event 441441, deploy outside the merge-triggered publication path, or mutate unrelated remote resources."
  - "Do not begin P14-02, Phase 15, another event admission, or unrelated repository work."
