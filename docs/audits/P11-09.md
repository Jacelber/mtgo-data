# P11-09 — Owner-approved skip and Phase 19 retirement route

The owner skipped the planned shared `metrics.py` extraction on 2026-08-02.
The production front end already presents literal win rate, while the existing
draw-adjusted calculation survives only in compatibility data, Schemas, tests,
protected event `434455`, and retained legacy JavaScript. Extracting a shared
API for that calculation would create throwaway work before its selected
retirement.

DEC-078 keeps all current production bytes and behavior unchanged during Phase
11. P11-10 is the next task and requires separate owner authorization. The
first Phase 19 compatibility-migration task will remove the draw-adjusted
calculation through an explicit Schema and generated-data migration, including
cross-product regression and rollback evidence. No code, Schema, generated
data, front-end source, workflow, or protected `434455` byte changes in this
skip record.

Local validation passed: the STATUS authorization contract parsed, all three
publication-governance documentation tests passed, repository validation
passed for 145 Python files, 1,636 JSON files, 23 YAML files, 30 references,
and 1,959 hygiene checks, and `git diff --check` passed.

The owner accepted this documentation record and authorized commit, remote
publication, and merge on 2026-08-02. P11-10 and the Phase 19 migration remain
separately gated.
