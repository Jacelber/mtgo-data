# P14-02A shared third-format MTGO repair

## Exact task contract

Cloud baseline: `48ab5e3d14f0a2e90a8aa71049966b06525d1079`, verified from
GitHub `Jacelber/mtgo-data` master on 2026-09-05 before the independent clone.
The checkout equals that SHA; the protected local master was not used.
Branch: `codex/p14-02a-shared-third-format`.

The Owner explicitly resumed Phase 14 and authorized only P14-02A. The
baseline contains P14-01 PR #313, Weekly Maintenance V2 PR #356, reviewed
MTGO publication PR #358, and weekly completion closeout PR #359. Their
Git history is authoritative; the old reset pause in STATUS is superseded by
the active conversation. P14-01 classification semantics remain accepted.

Problem: generic MTGO generators always selected a reviewed public population,
even for private executable formats; statistical Schemas and manifests named
only Standard/Modern; production used literal format lists; catalog hiding
did not stop recursive Pages copies of private statistics and reports.

Scope: repair shared generation, statistical contracts, reviewed population
selection, complete catalog admission, production routing, and Pages exclusion
using synthetic input. The current Owner instruction explicitly includes the
bounded production and Pages changes previously deferred by P14-00. No new
workflow, renderer, statistical engine, lifecycle state, or public product is
introduced. Artifact impact: `public_path`.

Expected effect: a private executable third format can produce local candidate
statistics and reports without a public-admission claim. It remains unavailable
in the catalog and absent from the Pages file set. Public formats require
complete capabilities and products, and still use only reviewed event IDs.

Recommended model: the current capable reasoning model; no agent delegation.

Exact additions declared before writing (all other changes modify existing
shared owners and their coupled tests or documentation):

- `tests/test_mtgo_third_format.py` (`code`);
- `docs/audits/P14-02A.md` (`docs`);
- `docs/history/ROADMAP-PHASE-14-COMPLETED.md` (`docs`).

Protected scope: all real data, generated statistics/reports, classifier rules,
Pauper capabilities, event whitelists, human Landing/name content, browser
assets, frozen Pickup behavior, public source archives, and Melee event bytes.
No collection, real Pauper generation, production dispatch, Pages deployment,
commit, or remote write occurs before local Owner acceptance. P14-02B and all
other development remain separate tasks.

Owner acceptance authorizes unchanged-task commit, one Ready PR, required CI,
and merge without repeated gates. It never authorizes a manual production or
Pages dispatch. The existing relevant-master Pages trigger includes this
packager change; therefore a later merge would automatically deploy the site.
This local handoff performs neither merge nor deployment. If the Owner's
no-Pages instruction is intended to persist through merge, that constraint
must be reconciled before the first remote write; silently disabling deployment
or changing repository settings is not part of this task.

## Refreshed shared invalidation map

| Contract / owner | Observed gap and operation | Coupled boundary / proof |
| --- | --- | --- |
| `src/mtgmeta/config.py`, `catalog.py` | Reuse one required capability set; public formats require executable state, complete capabilities and four required MTGO product files | Planned/private catalogs have no available product or default; incomplete public rejects |
| `mtgo/stats.py`, `matchup.py`, `top8.py`, `completeness.py` | Select retained input for a private executable definition; preserve explicit admitted membership for public generation | Synthetic two-event private population versus one admitted public event; no formula or classification change |
| `mtgo/metadata.py`, `classification_reports_cli.py` | Private metadata retains supported unbound 1.0; private reports retain 1.1 `all_available_events`; public 1.1/1.2 bindings and scope remain unchanged | No private claim of reviewed publication; exact public event count and frontier |
| `mtgo/__init__.py`, `mtgo/publication.py` | Reject public scope resolution for non-public formats; guard all private generator destinations, including overrides | All seven private producers reject a public output tree before writing |
| Statistical Schemas and `common.schema.json` | Replace active two-format enums with stable format IDs; extend the non-Standard hierarchy branch without removing Standard aliases | Generate synthetic range/decks, matchup, Top 8, completeness, hierarchy, metadata and reports; validate each generated file |
| `schemas/manifest.json`, `validate_schemas.py` | Replace duplicated statistical/report path mappings with wildcard format slots; require registered executable identity matching the path | Third-format files cannot evade mapping; mismatched/unregistered formats reject; full current manifest validates |
| `validate_repository.py` | Discover classifier-closure changes from generic format output/input paths | A third-format path selects the existing read-only closure owner; no generation during validation |
| `validate_production_candidate.py`, `.github/workflows/update.yml` | Derive collection and complete public product lists from the validated registry before fetch/build; private products cannot enter candidate write scope | Four-state selector and workflow ordering; existing permissions, immutable transfers and publication gates preserved |
| `build_pages_artifact.py`, `.github/workflows/pages.yml` | Apply registry/public/catalog agreement to both recursive and explicit statistics/report file admission; track the new inputs and install existing pinned runtime dependencies | Private/unregistered format outputs excluded; stale catalog and incomplete public format reject; 434455 compatibility closure preserved |
| STATUS, ROADMAP and phase history | Resume durable Phase 14 program, keep private generation unopened, move only already-completed P14-00/P14-01 detail to history | Live-state and resolvable history-pointer checks |

This map supersedes only the applicable implementation observations in the
2026-08-30 P14-00 inventory. Historical counts and migration tools are not
modified. Complete-public synthetic fixtures model an already-approved generic
product using explicit fixed membership; they do not grant a first real
Pauper grandfathered scope or claim human Landing acceptance.

## Four-state result

| Synthetic state | Local execution | Production product selection | Catalog and Pages |
| --- | --- | --- | --- |
| Planned | Rejects product capability | Excluded; prior archive capability retained | Unavailable; statistics/reports excluded even if files exist |
| Private executable | Shared producers succeed on retained synthetic inputs | Excluded | No available product/default; no direct statistics/report path |
| Incomplete public | Not an admission subject | Rejects missing capability/product | Catalog and Pages fail closed |
| Complete public | Shared producers use explicit approved membership | Selected dynamically | Required products available together; direct paths admitted |

The Pages policy continues to allow retained `data/` and `data_raw/` archives.
It is a website publication boundary, not a public-Git confidentiality claim.
The registry itself, real generated files, and public URLs are unchanged.

## Proportional verification and evidence

Python 3.12.14 runs from a new task-local `.venv`, populated anonymously from
PyPI using the exact existing `requirements-dev.txt` pins. No dependency
manifest or global Python installation changed. Pytest temporary repositories
are process-specific siblings outside the task checkout; no network or real
event input is used by the new synthetic tests.

- New synthetic generation, four-state admission, manifest-negative, and seven
  output-override guards pass. Fixed fixture capabilities are independent of
  the production selector's constant.
- Existing reviewed-publication membership and metadata regressions pass;
  they preserve Standard/Modern approved-event behavior and binding semantics.
- Four relevant workflow nodes pass: dynamic format routing, Pages input and
  evidence gates, immutable production transfer, and Landing/build order.
- Generic closure routing and the two affected manifest-selection nodes pass.
- Complete Schema validation passes for all 110 currently declared documents.
- The real baseline Pages selection has 2,743 files and retains all 494
  protected compatibility files. No selected path is under `stats/pauper/`
  or `reports/pauper/`. This is offline selection/compatibility evidence;
  no cache fetch, overlay build, upload, or deployment was performed.

An initial synthetic Top 8 test incorrectly supplied a catalog-only placeholder
to the producer. It now starts without that fixture file. A pre-existing
metadata unit test also needed the local Schema registry after the new shared
format-ID reference; it now resolves entirely offline. Only affected evidence
was rerun. No failure was accepted by weakening a Schema or public boundary.

Final local checks pass: 49 distinct risk-related pytest nodes, 110 declared
Schema documents, and changed-scope repository validation covering 42 paths
(Python 18, JSON 14, YAML 3, references 93, hygiene 42). Code evidence is reused
for later documentation-only closure. Complete diff/path review confirms no
protected data/rule/generated file changed. The live CLI selects `standard
modern` products and the same six collection formats as the existing registry.
The exact final file digests and local review patch are retained under the
ignored `.codex-local/` directory for Owner handoff.
No unbounded pytest, real Pauper corpus replay, UI/browser suite, production
candidate generation, or remote workflow dispatch is warranted for this task.

## Remaining boundaries

P14-03 owns human Landing, bilingual names, readiness/editorial carriers and
image-cache format generalization; their two-format assumptions remain visible
in the P14-00 inventory. P14-02B requires separate authorization before changing
the real Pauper registry or generating data. P14-07 must verify the accepted
shared production/Pages boundary against the eventual complete accepted
candidate, and still owns real admission and publication. No new product or
statistical decision is required for this synthetic shared repair.

Next step: Owner reviews and accepts this local task. Do not start P14-02B.
