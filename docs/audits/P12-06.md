# P12-06 chart semantics audit

## Authorization and boundary

The owner authorized local P12-06 implementation on 2026-08-08 after selecting
the reviewed B prototype. The isolated workspace is
`C:/tmp/mtgo-data-p12-06-20260808-01`, branch
`codex/p12-06-chart-semantics`, based on merged master commit
`0b8464d2a5dcbd354d3520255d0dc7764cc7ccf4` (PR 191). Artifact impact is
`user_visible_ui`.

The task changes only the MTGO statistics chart and its deck-identity
presentation. It does not authorize a product rename, statistical formula,
generated JSON, Schema, catalog, public path, workflow, Tabletop statistic,
production dispatch, commit, remote publication, merge, or follow-on task.

## Implemented result

- Preserved the Chinese product name `MTGO占比统计` and the existing source
  explanation that official MTGO publication is not a complete metagame.
- Removed both rank-colored pie charts and their slice-specific JavaScript.
- Added one horizontal high-score-share composition bar matching the accepted
  Landing contract. Current-week archetypes at or above the fixed 3% threshold
  remain interactive segments; lower classified share is labelled `Other`,
  `Unknown` stays separate, and genuine unassigned remainder remains explicit.
- Kept Top 8 share, counts, conversion, and every exact value in the
  authoritative table. The chart does not introduce a new metric or a second
  Top 8 visualization.
- Used the owner-supplied first representative card as the fill for six explicit
  Standard archetype IDs. This changes only the fill: tooltips contain deck name
  and share but no card name, and unmapped decks retain the accepted
  high-contrast Landing palette.
- Reused the accepted W/U/B/R/G mana SVG sources and added a local colorless
  symbol. The table, card-art map, and composition strip use explicit
  format-and-stable-ID mappings; no runtime name inference changes classifier
  or statistical data.
  Parent rows with maintained variants use only their common maintained color
  core, while subtype rows use the more specific reviewed mapping. `Unknown`
  intentionally has no color identity.
- Added Chinese and English chart, threshold, interaction, and accessible
  mana-label copy. The chart has no legend. Desktop uses one-click detail
  expansion; mobile uses first tap for disclosure and second tap to expand the
  same detail beneath the corresponding row.

No generated data, statistical formula, Schema, workflow, catalog, Pages
policy, public JSON path, or protected event 434455 byte changed.

## Verification

- JavaScript syntax checks passed for every modified script.
- P12-06, front-end split, P12-05, and live-status regression tests passed:
  `26 passed`.
- The ordinary pytest shard passed: `747 passed, 8 deselected`.
- The committed-baseline pytest shard passed: `8 passed, 747 deselected`,
  including byte-identical Standard and Modern statistics, matchups, Top 8,
  classification reports, and Phase 3 product regeneration.
- The complete Playwright suite passed: `37 passed`. This covers Chinese and
  English, Standard and Modern MTGO, Modern Tabletop, unavailable Tabletop
  redirect, desktop, 390-pixel, and 412-pixel viewports, URL-state and
  lazy-loading contracts, card-art loading, desktop expansion, and mobile
  first-tap/second-tap behavior.
- The browser loaded all referenced mana and representative-card assets with
  HTTP 200, rendered no pie or legend, kept Standard and Modern within the page
  viewport, and reported no application error.
- Separate prototype QA loaded the final B and revised Landing reference with
  no failed request or page overflow. B rendered six card-art fills; Landing
  retained its five existing mapped rows plus Other. Every chart tooltip showed
  only deck name and share.
- A separate subtype browser assertion verifies the Esper Goryo's Reanimator
  W/U/B symbols and accessible Chinese label. It also caught and drove the fix
  for the single-active-subtype row initially inheriting the parent U/B core.
- Ruff, strict mypy, three native Node tests, repository validation, rule
  validation, Schema validation, and `git diff --check` passed. Repository
  validation checked 133 Python, 12 JavaScript, 1,691 JSON, 28 YAML, 51
  reference, and 2,048 hygiene entries; Schema validation checked 73 public
  generated documents. The pinned CI actionlint executable was unavailable
  locally, and no workflow file changed in P12-06.

The final diff review found no generated data, Schema, workflow, catalog,
public-path, Tabletop statistic, or protected event 434455 change.

## Stop point

Stop after local implementation, complete technical acceptance, and owner
review. Commit, push, pull-request creation, merge, deployment, P12-07, P12-08,
P12-08A, classifier remediation, and P12-10 require their applicable separate
owner authorization.
