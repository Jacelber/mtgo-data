# P14-02B private Pauper MTGO candidate

## Exact task contract

Cloud baseline: `df5ac89706bd5d04a0e08996173e909bf6c87d5e`, verified as
GitHub `Jacelber/mtgo-data` master on 2026-09-05 before this branch was
created. The SHA is the merge commit for P14-02A pull request #360. Its
post-merge pull-request validation run `33941314135` and automatic Pages run
`33941314151` both passed. The branch is
`codex/p14-02b-private-pauper` in a separate Codex worktree; the protected
local master was not used.

After the candidate exposed the shared public/private closure gap, the Owner
separately accepted and merged P14-02A-R1 through pull request #361. On the
Owner's later explicit P14-02B resume instruction, this branch fast-forwarded
without path overlap to GitHub master
`771f9d5ae3f3555e8398ff53613ac0d4f6fc8967`. The retained candidate paths and
generated bytes were not regenerated or rewritten by that refresh.

The Owner explicitly resumed Phase 14 and authorized only P14-02B. P14-01's
Pauper taxonomy and P14-02A's shared third-format contracts remain unchanged.

Problem: the retained Pauper archive was not an executable product input and
had no versioned private statistics, matchup, Top 8, completeness, hierarchy,
metadata, or quality candidate. It also had no retained Videre match archive,
which had to remain a visible coverage limitation rather than become inferred
match data.

Scope: activate only the capabilities required by the P14-02B generators while
keeping `public: false`; generate the private candidate from retained inputs;
update the generated consumer catalog without making a Pauper product
available; validate the actual generated documents and Pages exclusion.
Artifact impact: `internal_diagnostics`. No product path, statistical formula,
classifier rule, source archive, Schema, manifest, workflow, browser asset, or
Landing carrier changes. No file is deleted or renamed.

Expected effect: a Schema-valid Pauper MTGO candidate records its input range,
classification quality, missing matchup coverage, Top 8 source state, and
hierarchy while the consumer catalog and Pages artifact expose no Pauper
product.

Recommended model: `gpt-5.6-sol`, high reasoning. No agent delegation is
required.

## Retained input selected at the task base

The private generators dynamically selected every retained exact-format Pauper
event rather than a frozen count or name list:

- 106 event documents dated 2026-04-02 through 2026-09-03;
- 3,387 retained player/deck records;
- 99 Pauper Challenge 32, three Pauper Showcase Challenge, one Pauper
  Challenge 16, one Pauper RC Qualifier, one Pauper RC Super Qualifier, and
  one Pauper Showcase Qualifier;
- zero empty main decks, zero empty sideboards, zero invalid Swiss scores, and
  zero invalid final ranks; and
- zero files under `data/pauper/mtgo/matches/`.

The two events in incomplete week 2026-W36 remain retained input and appear in
the complete-corpus classification reports. They do not enter range, Top 8, or
completeness products whose latest complete week is 2026-W35.

No event or matchup was fetched, refreshed, synthesized, or deleted.

## Registry and generated candidate

Pauper is now `state: executable`, `mtgo.enabled: true`, and `public: false`.
Its enabled product capabilities are exactly:

- `classification`;
- `event_statistics`;
- `range_statistics`;
- `matchup_statistics`;
- `weekly_top8`;
- `completeness_reporting`;
- `metadata_generation`; and
- `catalog_generation`.

`landing_generation` remains disabled because P14-03 owns the Landing review
carrier and human content. Existing event-collection authorization remains
unchanged and was not exercised.

The shared generators wrote 30 Pauper documents: 24 under
`stats/pauper/mtgo/` and six under `reports/pauper/mtgo/`. The statistical tree
contains the range and representative-deck catalog, four range/deck pairs,
four matchup ranges and their catalog, four completeness ranges and their
catalog, the first 2026-W35 Top 8 week and comparison base plus their catalog,
the maintained hierarchy, and unbound private metadata. The report tree
contains the classification index, Unknown, multiple-match, overridden-match,
conflict, and subtype documents.

| Range | Period | Decks | High score | Top 8 | Unknown |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 week | 2026-08-24 through 2026-08-30 | 192 | 109 | 48 | 4 |
| 4 weeks | 2026-08-03 through 2026-08-30 | 608 | 368 | 152 | 10 |
| 12 weeks | 2026-06-08 through 2026-08-30 | 1,980 | 1,243 | 496 | 26 |
| 36 weeks | 2025-12-22 through 2026-08-30 | 3,324 | 2,065 | 832 | 40 |

The maintained classifier subject is
`d7f76feea5b5e7c882ede8a184fa7f335b115c6d5e0f9f27b2d365cab5876f61`.
The hierarchy contains 64 parents, 71 leaves, and three expandable parents.
Every classification-dependent candidate family carries this same digest.

The first Top 8 candidate establishes 2026-W35 only, following the existing
first-generation lifecycle rather than inventing a historical backfill. It
contains six events and 48 available ranked decks, including one explicit
Unknown identity and zero unavailable decklists. The week is provisional
through 2026-09-06 and seals on 2026-09-07; this status is retained in the
candidate rather than rewritten as final.

## Quality and missing-source result

The complete-corpus classification report records 3,387 decks: 3,344
classified, 43 Unknown, zero conflicts, and zero invalid decks. It also records
1,174 multiple matches resolved through the accepted priority contract, 1,174
corresponding overrides, 239 selected subtypes, and 39 same-parent
multiple-subtype diagnostics. Strict classification validation passes because
Unknown remains visible and non-blocking while conflicts and invalid decks are
zero. P14-02B does not reinterpret or repair any taxonomy result.

No Pauper Videre archive exists. Therefore all four matchup documents contain
zero counted matches and sample-dependent rates remain unavailable. The
completeness denominator is nevertheless known from the retained official
events, so the documents truthfully report an available 0.0 coverage rate:

| Range | Expected events | Available | Missing | Coverage |
| --- | ---: | ---: | ---: | ---: |
| 1 week | 6 | 0 | 6 | 0.0 |
| 4 weeks | 19 | 0 | 19 | 0.0 |
| 12 weeks | 62 | 0 | 62 | 0.0 |
| 36 weeks | 104 | 0 | 104 | 0.0 |

High-score decklist completeness has zero unsupported events. Its
observed-versus-modeled rates are 0.943723, 0.926369, 0.780029, and 0.811953
for the 1-, 4-, 12-, and 36-week ranges. These are the existing documented
model outputs; no missing source was converted into a zero decklist or an
estimated match.

## Public isolation

The regenerated `stats/catalog.json` records Pauper's executable registry
state but gives all five products `available: false`, null paths, and no
default product. Standard and Modern catalog product availability is
unchanged.

Applying the actual `configs/pages_publication.json` policy to the exact local
tree selects 2,743 files and selects zero path under `stats/pauper/` or
`reports/pauper/`. The global `stats/catalog.json` remains selected and agrees
with the registry. The pre-existing public archive policy for `data/pauper/`
is unchanged; this task adds no source byte and makes no confidentiality claim
about already retained public-Git data.

No Pages build, upload, deployment, production run, or manual dispatch was
performed.

## Proportional verification

- Strict real-corpus classification report generation: PASS, 3,387 decks,
  43 Unknown, zero conflicts, and zero invalid decks.
- Changed-document Schema validation: PASS, 31 mapped JSON documents.
- Current-candidate consumer invariants: PASS, nine Pauper-only nodes covering
  representative-deck provenance, hierarchy-aware zero-sample matchups,
  range/completeness agreement, and Top 8 digest/lifecycle consistency.
- Actual Pages selection and catalog agreement: PASS, zero selected Pauper
  statistics or report paths and no available Pauper product.
- Final changed-scope repository validation after the R1 refresh: PASS for 35
  candidate paths, including 31 JSON documents, two YAML documents, 107
  references, and 35 hygiene subjects.

P14-01's unchanged rule contract and P14-02A's unchanged Schema/manifest,
production-routing, and Pages-boundary evidence are reused rather than rerun.
The P14-02A synthetic third-format fixture did not call the shared repository
classifier-closure discovery boundary, so its prior pass did not cover this
failure.

## Resolved shared blocker

P14-02A-R1 made complete-public qualification a pure registry, capability, and
required-file calculation shared by catalog generation, classifier closure,
and repository catalog consistency. Default closure discovery now excludes a
private executable format, while explicit private selection fails closed before
output. The stored catalog remains a derived consistency target rather than an
admission authority.

R1 changed no P14-02B candidate path. Its exact pull-request and post-merge CI
completed successfully before this branch refresh. The only invalidated P14-02B
evidence is the final repository validation and the direct Pages selection
check named by the Owner.

Both resumed checks passed on the refreshed candidate. The actual Pages policy
still selects 2,743 files, exposes only Standard and Modern statistics/reports,
and selects zero path under `stats/pauper/` or `reports/pauper/`.

## Stop point

The generated private candidate remains retained and stops for Owner acceptance
after the two resumed checks pass. P14-02B performs no Landing review,
bilingual-name import, Paupergeddon trial, public admission, production
dispatch, or manual Pages dispatch. A future P14-03 task remains separately
unauthorized.
