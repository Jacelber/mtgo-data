# P12-05 shared visual foundation audit

## Authorization and boundary

The owner authorized local P12-05 development on 2026-08-08. The isolated
workspace is `C:/tmp/mtgo-data-p12-05-20260808-01`, branch
`codex/p12-05-shared-visual-foundation`, based on merged master commit
`91f81dc282af09f52bd3e1993e680fe0f0c5c977` (PR 190). Artifact impact is
`user_visible_ui`.

After visual review, the owner accepted the P12-05 result and separately
authorized commit, Ready pull-request publication, merge, and deployment. The
owner also approved recording P12-08A as a later route correction. This does
not authorize P12-06, P12-08A implementation, classifier remediation, or
P12-10.

## Implemented result

- Published the accepted Editorial Analysis Console palette and shared type,
  spacing, density, radius, focus, elevation, control, table, link, and
  responsive tokens in the existing classic static CSS.
- Migrated `/index.html` and `/melee/index.html` to one shared semantic header:
  the Chinese title is `猫猫万智周报`, the accepted direction-B cat art is a
  decorative local watermark, format controls are bordered pills, language is
  small borderless text, and top-level products are a separate navigation row.
- Added a visually distinct `.section-tabs` foundation for future in-page
  Landing navigation without introducing Landing product behavior early.
- Preserved every existing product ID, route, query parameter, availability
  decision, loading request, product order, and current Weekly Pickup entry.
- Kept the production cat asset byte-identical to the accepted P12-04A source
  and recorded its provenance and SHA-256 under `assets/images/README.md`.
- Updated language switching so visual selection and `aria-pressed` state stay
  synchronized.

No generated data, Schema, statistics, workflow, catalog, Pages policy, public
JSON path, or protected event 434455 bytes changed.

## Accepted route correction

P12-08A now explicitly covers the existing statistics, Tabletop deck, Top 8,
and Weekly Pickup lists that were not assigned a complete small-screen
implementation task. At 780 CSS pixels and below, one-dimensional lists become
semantic cards without dropping data, and detail opens below its originating
card. Truly two-dimensional matrices and cross-event comparisons retain
bounded horizontal scrolling with a sticky identity column. Implementation
remains gated behind accepted P12-08 and separate owner authorization.

## Browser finding

The first 390-pixel regression exposed a pre-existing cascade conflict:
`#payload-status` overrode the shared screen-reader-only margin and a mobile
rule expanded the hidden status element to page width, producing 10 pixels of
root horizontal overflow. P12-05 restored the one-pixel hidden geometry and
added a page-level overflow assertion. Wide data tables and product navigation
remain bounded scrollers rather than widening the page.

## Verification

- Ordinary pytest shard: `742 passed, 8 deselected`.
- Committed-baseline pytest shard: `8 passed, 742 deselected`; all selected
  generated products remained byte-identical.
- P12-05, live-status, history, and existing static-asset targeted tests:
  `20 passed`.
- Full Playwright suite: `34 passed`; its production-page matrix contributed
  `24 passed` across Chinese and English,
  Standard and Modern MTGO, Modern Tabletop, unavailable Tabletop redirect,
  desktop, 390-pixel, and 412-pixel viewports.
- Final clean-workspace repository validation passed with Python `132/132`,
  JavaScript `12/12`, JSON `1691/1691`, YAML `28/28`, references `51/51`, and
  hygiene `2033/2033`.
- Rule validation passed for `my_archetypes/standard.yaml`; Schema validation
  passed for all 73 public generated documents.
- Ruff, mypy, the native Node suite (`3 passed`), and `git diff --check` passed.
- Local visual review captures were generated from the isolated workspace at
  `C:/tmp/p12-05-review/` for Modern MTGO desktop/390 and Tabletop 412.
- After the accepted P12-08A route correction was recorded, its focused
  documentation and visual-foundation tests passed (`16 passed`) and the full
  repository validator remained green.

## Stop point

After the authorized commit, Ready pull request, merge, and Pages verification,
stop. Do not begin P12-06 or P12-08A automatically.
