# P13-06R corrected non-public real-event validation

Date: 2026-08-27

Base: `3a571a0780c7861d3f0cd4422368513ca45d8c22` (`master`)

Branch: `codex/p13-06-real-event-437444`

Status: Corrected local validation complete; pending Owner acceptance

Artifact impact: `internal_diagnostics`

## Owner-supplied subject and exact test scope

The Owner supplied and authorized only Melee event `437444`,
`https://melee.gg/Tournament/View/437444`. The public tournament page identifies
it as `NRG Series: Magic $2.5k Modern Trial`, a completed Tabletop Modern event
on 2026-08-23 with 99 joined players, seven Swiss rounds, and a cut to Top 8
single elimination.

The truthful NRG Series category first proved the production whitelist's
fail-closed boundary because it is not an approved product series. The Owner
then explicitly admitted this exact event to the disposable test scope only,
noting that recent approved-category events are unavailable and older events
would require materially larger classifier adjustment. After the first test
incorrectly made retained event `434455`'s historical taxonomy authoritative,
the Owner authorized P13-06R to replace that result with disposable projections
of both events under the current approved Modern classifier. The correction did
not authorize the permanent active-taxonomy admission rule.

The exception was implemented only through an uncommitted test bootstrap bound
to event ID `437444` and a temporary registry containing that event plus the
unchanged production definition of reference event `434455`. It did not add
NRG Series to the production parser, Schema, whitelist, catalog, Pages artifact,
front end, or another event's test scope.

## Collection and privacy result

A fresh disposable clone was created from the exact base and its push URL was
disabled. The collector used a process-generated 32-byte disposable HMAC key
and non-secret key ID `p13-06r-437444-disposable-20260827-01`. The key was never
printed or persisted and was cleared when the collection process ended.

The minimized v3 collector completed its frozen plan:

- 120 of 120 responses;
- 411,818 persisted bytes;
- one tournament resource;
- 10 completed rounds;
- 99 standings/decklist subjects and 99 decklists; and
- 273 source matches.

Every resource passed its strict resource Schema and prohibited-field scan
during persistence and again during retention parsing. The complete manifest,
file set, byte counts, response digests, event identity, request methods,
round/decklist context, and event-scoped participant references reconciled.
No source participant ID or HMAC key material entered the retained snapshot.

Retention produced a valid normalized event with seven Modern Swiss rounds
and three Modern playoff rounds. No round or result remained unknown. The
playoffs stayed contextual and outside `all_constructed`; byes and intentional
draws remained explicit exclusions rather than being coerced into played
matches.

## Corrected current-classifier result

The exact base's current Modern taxonomy digest was
`65fe8be8356aac826bbadfe01d7ae603a63aa44c9880a232c6ac8e9b7cfeaf58`.
P13-06R copied the byte-identical retained normalized event `434455` into the
disposable projection and rebuilt both events' classification overlays,
opportunity ledgers, statistics, matchups, metadata, and catalog-admission
evidence with that same taxonomy.

Current-classifier results were:

| Event | Deck records | Classified | Unknown | Conflicts | Invalid |
| --- | ---: | ---: | ---: | ---: | ---: |
| `434455` | 362 | 362 | 0 | 0 | 0 |
| `437444` | 99 | 95 | 4 | 0 | 0 |

All 362 selected `434455` parent/subtype identities remained identical to its
older published classification. Its digest still had to advance because the
maintained taxonomy and complete hierarchy had evolved. Event `437444`
retained:

- 95 classified decks;
- 4 explicit Unknown decks;
- no conflicts or invalid decks; and
- passing strict classification.

The committed event `434455` metadata remains protected under taxonomy digest
`dbb77e51b1ab2366cd6a6da7a81dfb3c9bb1ebb287de3836c9f11b6ad09cff6f`, while
the current digest is the value above. The original mixed-digest rejection was
correct. The subsequent historical-taxonomy positive result was diagnostic
only and is expressly superseded: P13-06R did not load or use the historical
taxonomy from commit `e6283d265fc3dcbf45f6abd18a7877f266a94e09`.

The opportunity ledger contained 693 participant opportunities and admitted
260 physical Swiss matches. The single-event quality status was `warning` only
for the four explicit Unknown classifications; it was non-blocking. Its six
excluded physical matches were three byes and three intentional draws. No
unknown result, no-show, administrative result, disqualification, or Top 8
lock award entered the matrix.

## Positive multi-event aggregation

The versioned contract admitted current-classifier projections of real events
`434455` and `437444` after active
Schema, source, format, quality, scope, taxonomy, identity, path, digest, and
catalog-evidence validation. The canonical identity order came from the
complete current taxonomy plus the stable Unknown identity, not from either
event's observed presentation order.

The raw-count result reconciled as follows:

| Measure | `434455` | `437444` | Combined |
| --- | ---: | ---: | ---: |
| Included physical matches | 1,394 | 260 | 1,654 |
| Directed observations | 2,788 | 520 | 3,308 |

The combined subject contained 1,682 source matches, 28 excluded matches, 43
observed parent identities, and 81 observed leaf identities. It retained 790
non-zero low-sample leaf-matrix cells with the shared threshold of 20.

The following checks passed:

- combined counts equal the sum of the two event inputs;
- every directed cell has an inverse W-L-D cell;
- leaf counts roll up exactly to parents;
- directed observations equal twice the physical-match count;
- literal rates equal `wins / (wins + losses + played draws)`;
- non-zero samples contain valid 95% Wilson intervals;
- zero samples remain null and are not mislabeled as low sample;
- non-zero samples below 20 carry the low-sample warning;
- the Unknown identity remains present;
- the combined document passes its versioned Schema;
- both event metadata and matchup inputs, catalog 1.1 evidence, and the combined
  document pass their six active Schema validations;
- all nine protected `434455` closure files remain byte-identical to compatibility
  contract `1.6.0`;
- no Overview metric is present; and
- no MTGO record entered the calculation.

The first corrected harness attempt omitted the stable explicit Unknown identity
from the maintained taxonomy hierarchy. The second used the single-event
publisher's one-event catalog as if it accumulated events. Inspection confirmed
both were disposable harness composition errors: the accepted canonical
hierarchy is the maintained taxonomy plus stable Unknown, and P13-02 composes
separately verified event entries into catalog 1.1 evidence. Only those failed
harness steps were rerun. Product code, statistical meaning, source inputs, and
the successful per-event evidence did not change.

## Verification boundary

This correction exercised only the invalidated taxonomy-cohort subject through
current classification, opportunity construction, event statistics, event
matchup, catalog admission, and multi-event aggregation. It did not repeat the
synthetic P13-01 through P13-05 Python, Node, or browser suites. Final document-
contract and changed-scope repository validation are sufficient for the
retained documentation-only diff after disposable material is removed.

## Protected boundaries and cleanup

The committed production whitelist, approved series enumeration, Schemas,
classifier, normalized data, event statistics, catalogs, global catalog, Pages
artifact, front-end code, workflows, and retained event `434455` bytes are
unchanged. The test registration, category bootstrap, HMAC material, minimized
snapshot, normalized event, classifications, opportunity ledger, statistics,
current-taxonomy projections, combined result, disposable validation scripts,
and dependency environment were removed after the non-sensitive counts above
were recorded. No historical taxonomy was copied during P13-06R.

No MTGO data, credential, Issue mutation, production dispatch, remote write,
public enablement, or Phase 14 work occurred.

## Stop point

Present this corrected local P13-06R result and documentation-only diff to the
Owner for acceptance. Do not commit, publish, merge, enable event `437444`,
dispatch production, or begin Phase 14 before the applicable Owner decision.
