# P11-07 — Pinned actionlint workflow validation

P11-07 adds `actionlint` 1.7.12 to the existing read-only CI static-validation
job. It validates every workflow without changing permissions, triggers,
branches, publication behavior, production data, or product behavior. Existing
Python workflow-contract tests remain the authority for those project-specific
boundaries.
