# P8-07 backend consumer-readiness audit

Date: 2026-07-28

Audited base: `024c63afc390201bc4fbadfd09e033c538624270`

## Result

The retained Standard, Modern, and Tabletop payloads passed their existing
focused tests and repository validation, but the frozen Phase 8 consumer
contract was not yet implementable without browser-side derivation.

The blocking gaps were:

1. subtype names were short labels in range, deck, hierarchy, matchup, and
   Tabletop documents;
2. Tabletop overview and matchup records exposed only the legacy
   draw-adjusted percentage;
3. Top 8 exact decks had no produced deviation and referenced mutable
   `decks_4w.json`;
4. `stats/catalog.json` did not exist, so format/product availability required
   hard-coded paths.

P8-08 remains prohibited. The owner authorized the separate
`P8-07-BACKEND-CONSUMER-BRIDGE` task and selected immutable weekly Top 8
comparison bases.
