# P7-01 reference-event activation audit

## Purpose

P7-01 begins Phase 7 by activating the already verified mixed-format Modern
reference event without collecting or publishing its data. It also records the
complete P7-01 through P7-08 dependency order so later work cannot skip the
normalized, classification, statistical, or publication boundaries.

## Task contract

- Task ID: `P7-01`
- Branch: `codex/p7-01-reference-event-contract`
- Base commit: `ca641b3e96707fc8f1cb9945b188dd4019b605fd`
- Reference event: Melee `434455`
- Event format: Modern
- Event structure: mixed Draft and Constructed
- Allowed product change: enable this verified whitelist entry for explicit
  manual collection
- Prohibited in this task: live requests, real raw or normalized data,
  classification, statistics, catalogs, workflows, front-end changes, and any
  MTGO data or behavior change

## Implementation

`configs/melee_events.yaml` now sets only event `434455` to `enabled: true`.
Its reviewed URL, dates, phases, format, event structure, statistical defaults,
source evidence, and special handling remain unchanged.

The existing collection boundary still requires:

- an exact whitelisted event ID;
- `review_status: verified`;
- safe Melee URLs and identifiers;
- explicit `--execute` for any request;
- explicit `--complete` for complete DataTables discovery and collection.

No workflow calls the Melee client. P7-01 creates none of:

- `data_raw/melee/434455/`;
- `data/modern/melee/events/434455.json`;
- `stats/modern/melee/events/434455/`.

Historical Phase 5 tests now reproduce the disabled lifecycle state with an
immutable disabled event copy instead of incorrectly requiring the current
Phase 7 configuration to remain disabled forever. Independent current tests
prove activation, whitelist exclusivity, explicit execution, and no output
side effects.

## Product boundaries

- MTGO and Melee remain separate.
- Draft rounds remain excluded from Modern statistics.
- The Draft Top 8 remains contextual only.
- No statistical formula or classification rule changes.
- No real participant, match, or decklist data enters this change.
- P7-02 remains separately controlled.

## Validation

Validation completed in the isolated workspace:

- focused registry, Schema, Phase 5 lifecycle, and P7-01 activation tests:
  `30 passed`;
- impacted Melee subsystem: `107 passed`;
- complete pytest suite: `421 passed`;
- repository validation: PASS, including 89 Python files, 1,016 JSON files,
  16 YAML files, 30 references, and 1,196 hygiene entries;
- Standard and Modern rule validation: PASS;
- public-output Schema validation: PASS for all 46 mapped documents;
- diff and changed-path review: PASS;
- raw, normalized, statistical, cache, credential, and temporary artifacts
  added: none.
