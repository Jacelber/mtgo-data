# P10-08 Python 3.12 runtime alignment

## Scope and authorization

The owner authorized P10-08 local implementation on 2026-08-01. This task
aligns the scheduled MTGO production update with the Python 3.12 runtime
already exercised by local validation, pull-request and master CI, the manual
Melee workflow, and the Pages artifact workflow.

The isolated workspace is `C:/tmp/mtgo-data-p10-08-20260801-01`, branch
`codex/p10-08-python-312-runtime`, based on P10-07 merge commit
`a2d92c384d386d0a98ab9fd4bb7632ce066b3bfd`. Local implementation does not
authorize a production dispatch, data fetch, generated-data commit, Pages
setting change, storage migration, history rewrite, P10-09, or later work.

## Runtime decision

Python 3.12 is the sole supported project runtime. It is used for local
validation, GitHub Actions CI, the manual Melee workflow, Pages artifact
builds, and the scheduled MTGO production update. Python 3.11 is no longer a
supported project runtime, so no dual-runtime compatibility matrix is needed.

The production workflow retains its existing Python package pins,
clean-checkout baseline, candidate validation, publication confirmation,
schedule, concurrency, permissions, command order, and generated-path
boundary. The only executable production change is the setup action's runtime
selection from Python 3.11 to Python 3.12.

## Validation plan

Before the runtime change, the production-workflow structural suite passed on
Python 3.12. The test expectation was then changed to require Python 3.12 and
failed while the workflow still declared Python 3.11. After the workflow
change, the same suite passed again.

Final local acceptance must run the complete Python 3.12 test suite, repository
validation, rule validation, public Schema validation, and a diff check. These
checks do not fetch source data, regenerate production statistics, or publish
any remote change.

## Validation result

The focused production, CI, and Pages workflow suite passed 26 tests on Python
3.12.7. The complete Python 3.12 suite passed all 655 tests in 418.69 seconds.
Repository validation passed for 135 Python files, 1,613 JSON files, 22 YAML
files, 30 references, and 1,908 hygiene paths. Standard rule validation and
public Schema validation of all 69 documents also passed.

The scoped runtime test now proves that every project workflow using
`actions/setup-python@v6.3.0` selects Python 3.12. The production workflow's
schedule, permissions, command order, candidate validation, publication
confirmation, and generated-path staging scope remain unchanged. No external
source request, generated-data change, production dispatch, Pages deployment,
or front-end change occurred.

## Stop point

The owner accepted the local result on 2026-08-01 and authorized commit,
push, pull request, and merge. A real manual MTGO production workflow dispatch
remains separate authorization because it may fetch current source data and
publish a generated-data commit. Stop after P10-08 publication; P10-09 remains
a separately authorized fetch/build/publish workflow split.
