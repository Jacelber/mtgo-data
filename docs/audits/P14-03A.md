# P14-03A private MTGO review-scope contract

## Frozen subject

- Repository: `Jacelber/mtgo-data`
- Base branch: `master`
- Base commit: `c781db0a9b725ffc0a673f014394af79338bcfbb`
- Contract: `P14-03A / Definition v1`
- Expected artifact impact: `internal_diagnostics`

Definition, Implementation, and Completion are stages of this one task.
Implementation is limited to the paths enumerated in the accepted contract and
stops at a local artifact for Owner acceptance. Completion may begin only after
that acceptance. It must use the current publication gate and must not dispatch
production or Pages manually.

## Frozen implementation boundary

The implementation adds one small format/week scope validator; supports exact,
independent closed review weeks; isolates new private name and visual digests;
adapts existing Landing generation without public admission; emits private
readiness `1.8.0`; prepares one-format completion evidence; and passes one
explicit scope through card-cache subject, build, and verification. Existing
public Standard/Modern defaults and historical object bindings remain intact.

No real Pauper names, visuals, reviews, completion records, generated Landing,
public files, front-end code, workflows, event data, statistics, or admission
configuration may change. Synthetic YAML, JSON, XLSX, and image bytes exist
only in repository-external test temporary directories.

## Local acceptance evidence

Evidence is recorded against the final working tree after the focused synthetic
tests, both complete Schema manifests, changed-scope repository validation,
Ruff, mypy, full diff review, and file-operation inventory finish. No cloud CI,
commit, push, pull request, merge, production run, or Pages run is part of the
Implementation stage.

- `tests/test_mtgo_review_scope.py`: `9 passed`.
- `tests/test_landing_card_image_cache.py`: `10 passed` after correcting a
  synthetic fixture that lacked the newly required registry input.
- Public generated-output Schema manifest: `140` documents passed.
- Melee data Schema manifest: `6` documents passed.
- Changed-scope repository validation: `28` paths, `RESULT: PASS`.
- Ruff: passed for every changed Python path.
- Narrow configured mypy: no issues in `4` source files.

The first two attempts to capture the long public Schema validation summary
completed without returning buffered output from the command executor. The
same final tree was then run in a pollable terminal and returned exit code `0`
with the `140`-document PASS above. No test input changed between those attempts.
