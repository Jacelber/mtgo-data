# U6 整体实施验收

日期：2026-09-12。文档纠正后的同一个 U6 已获 Owner 明确验收，授权在原范围内连续完成 U7。
已有实现及有效验证保留；实际切换进度见 RESULTS，不将验收本身当作生产切换完成。

## 本次重新提交修正了什么

- ROADMAP、STATISTICS_SPEC、PROJECT_SCOPE、FRONTEND_DESIGN_SYSTEM 及相关 DATA_ARCHITECTURE
  已逐段清理固定旧赛制／全量回归、完整档案审计前置、已完成阶段的开工与发布审批。
  父子计数、缺失值、字段版本、双产品分离、公共路径、语言／响应式和图片来源等有效要求保留。
- 已只读核对并同步公开事实：Standard、Modern、Pauper MTGO；Modern Tabletop
  `405590`、`441441`、`434455`，Pauper `438329`。公开不代表 P14 已知缺陷已修复。
- [RESULTS](RESULTS.md) 已关闭被后续验证解决的旧待办，补全现有一次性迁移表的原章节与新位置，
  并集中定位最新恢复设计、当前准备差异和已有验证。U7 真正未执行的事项仍明确保留。

本轮仅修改文档，采用聚焦语义审阅与必要入口核对；没有重跑产品验证、改变实现或新增检查机制。

## 这次交付改变什么

| 你关心的问题 | 准备完成的新行为 |
| --- | --- |
| 验收后还不断要求批准提交、合并、发布 | 同一交付按已确认计划连续推进；只有新业务、范围、权限、治理取舍或不可替代信息才请你判断。质量失败不撤销修复授权 |
| 为无关测试、旧声明和证明链耗时 | 旧 Gate、全量调度、合并证据核对、预检令牌及其专用测试退出。按本次结果、受改核心机制和少量必要入口选择验证；必要失败仍不放行 |
| 成功做过的事情反复重做 | 检查按实际相关内容复用；采集 checkpoint 可以跨无关提交续作；已生成的 MTGO 候选直接集成，不因 master 变化重启整个流程，不制造空证据提交 |
| 旧整站候选覆盖另一产品的新内容 | 自动准备固定包含请求改动与已交付改动的当前来源；后续写入再核对实际部署基准。相关输入或输出冲突由 Codex 聚焦处理，不自动重测全部内容 |
| 回滚又变成一轮测试或机器审批 | 仅 Owner 指令触发恢复前版。使用归档的完整部署包，不重抓、不重分类、不恢复旧治理；工具只核对对象、包和未结束写入 |
| 回滚后旧故障又被自动更新发布 | 回滚附带暂停自动发布，采集和准备继续。明确要求恢复，或提前授权的解除条件已经完成，才解除；普通修复、合并或一次发布不自动解除 |
| 为新治理维护另一套繁重系统 | 三份职责规范、有限命令和一个部署控制记录；不建设通用依赖分析、签署台账、定期审计或新的后台任务 |

## 可直接审阅的成果

- [协作、授权和停止条件](../../GOVERNANCE.md)
- [结果检查、核心机制测试与复用](../../QUALITY.md)
- [候选、交付、归档与 Owner 指令恢复](../../DELIVERY.md)
- [具体实施结果、章节迁移及验证材料定位](RESULTS.md)
- [最新已批准的 Owner 恢复决定](RECOVERY_AUTOMATION_DECISION.md)；同步设计为 Step 6 v1.2、Step 7 v1.3

原 AGENTS、Claude、Copilot 和 PR 模板已经在准备分支整体替换。已批准的 12 份旧文件、
旧全量/预检/合并证明脚本及专用测试已删除。无调用的旧代码片段 fixture 和历史质量入口
也已退出；实际业务规则、分类样本、赛事配置和公共产品数据保留。原九份本地设计已改为
唯一仓库设计位置的指针，一次性迁移工具不随日常产品发布。

## 技术部分已有实际结果

1. **真实恢复基础已保全。**线上部署 `6394719184` 的原包完整保存在私有 GitHub 归档，
   实际上传和重新下载核对完成。U7 基线准备器只读运行成功，确认当前服务与该包仍对应；
   没有将源码重建物当作历史包，也没有宣称 P14 已知缺陷修复。
2. **云端写入与恢复已在独立目标执行。**实际完成合成版本部署、恢复、过期请求拒绝、
   Owner 指令恢复、恢复后自动发布暂停、明确解除与再次自动发布。记录见 RESULTS；
   这些操作均未改变正式产品。
3. **私有传递已执行。**完整候选的加密交接与取回已通过；新增周审材料交接运行
   [34675532942](https://github.com/Jacelber/mtgo-data-governance-verification/actions/runs/34675532942)
   也已实际完成加密、私有保存和取回比较。MTGO 未发布输出使用同一已验证的加密方式，
   生成作业不持有归档写入或解密凭据。业务周审材料独立于部署/恢复包，不公开全文。
4. **本次变化的衔接已经聚焦验证。**实际合成 Git 历史验证不覆盖并发交付、重复续作不重建提交、
   无关提交复用 checkpoint、相关冲突保留候选；实际检查入口发现人工小样的手算不一致而忽略无关格式损坏。
   写入终止但服务未知的分支保留真实失败与未知，不自动回滚。测试未执行和产品检查失败分开报告，
   两者均不能被当作必要验证通过。没有为收尾重跑产品全量测试。

## 验收后 U7 才执行的动作

- 在短切换窗口内暂停相关旧写入入口，查明已在途操作；站点继续服务现有版本。
- 重新核对当时实际线上对象，必要时保全较新的真实包，再初始化正式恢复基线。
- 提交、PR、必要修复、合并同一套准备成果并应用对应云端设置；保留历史防删除/非快进保护，
  不增加人工 reviewer。生产环境首次实际调用及实际入口确认在这一步完成。
- 将原项目检出的已跟踪内容快进同步至最终云端版本；已确认其已跟踪文件无本地修改，
  不清理未跟踪的预览、导出和工作区。确认新入口及新规则生效，恢复获准自动任务，
  清理本次临时验证目标并报告切换结果。之后按 Step 8 利用自然任务观察效果。

项目外 `.agents`、`.codex` 目录未发现额外项目指令，专用 skill 核查未发现另一个项目 Gate。
项目记忆替换请求已按授权提交；旧摘要是否重新生成尚未证实，不能说旧记忆文字已经物理清除。
当前项目入口已明确取消历史记忆的规则地位；U7 会检查实际重新加载的来源。若旧外部提示仍实质
控制流程，不能宣布统一切换完成。此处不要求你再次授权同一记忆更新。

远端请求结果完全未知时不能盲目重复写入；须先查询或处理平台事实。这是实际平台限制，
不是重新申请你的回滚批准，也不意味着重新测试产品。发生这种情况会说明具体剩余影响。

## 已完成的验收

Owner 已确认这套协作、质量、复用与恢复行为，U7 在原授权内连续完成。
本次验收不改变业务、统计、公开范围，也不包含 P14 图片或颜色修复。
实际入口、归档引用或生产结果未成立时，继续如实处理，不提前宣布整体完成。
