{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jacelber.github.io/mtgo-data/schemas/mtgo-matchup.schema.json",
  "title": "MTGO parent and hierarchical matchup statistics",
  "x-schema-version": "1.1.0",
  "type": "object",
  "required": [
    "schema_version",
    "format",
    "source",
    "classifier_digest",
    "period",
    "min_sample_hint"
  ],
  "properties": {
    "schema_version": {"const": "1.1.0"},
    "format": {
      "$ref": "common.schema.json#/$defs/formatId"
    },
    "source": {
      "const": "mtgo"
    },
    "classifier_digest": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "period": {
      "$ref": "common.schema.json#/$defs/period"
    },
    "min_sample_hint": {
      "type": "integer",
      "minimum": 1
    },
    "archetype_order": {
      "$ref": "#/$defs/legacyOrder"
    },
    "overall": {
      "$ref": "#/$defs/legacyRecordMap"
    },
    "matrix": {
      "$ref": "#/$defs/legacyRecordMatrix"
    },
    "hierarchical": {
      "const": true
    },
    "canonical_level": {
      "const": "leaf"
    },
    "hierarchy": {
      "$ref": "#/$defs/hierarchy"
    },
    "parent_order": {
      "$ref": "#/$defs/identityOrder"
    },
    "parent_overall": {
      "$ref": "#/$defs/recordMap"
    },
    "parent_match_records": {
      "$ref": "#/$defs/matchRecordMap"
    },
    "parent_matrix": {
      "$ref": "#/$defs/recordMatrix"
    },
    "leaf_order": {
      "$ref": "#/$defs/identityOrder"
    },
    "leaf_overall": {
      "$ref": "#/$defs/recordMap"
    },
    "leaf_match_records": {
      "$ref": "#/$defs/matchRecordMap"
    },
    "leaf_matrix": {
      "$ref": "#/$defs/recordMatrix"
    }
  },
  "oneOf": [
    {
      "properties": {
        "format": {
          "const": "standard"
        }
      },
      "required": [
        "archetype_order",
        "overall",
        "matrix",
        "hierarchical",
        "canonical_level",
        "hierarchy",
        "parent_order",
        "parent_overall",
        "parent_match_records",
        "parent_matrix",
        "leaf_order",
        "leaf_overall",
        "leaf_match_records",
        "leaf_matrix"
      ]
    },
    {
      "properties": {
        "format": {
          "not": {"const": "standard"}
        }
      },
      "required": [
        "hierarchical",
        "canonical_level",
        "hierarchy",
        "parent_order",
        "parent_overall",
        "parent_match_records",
        "parent_matrix",
        "leaf_order",
        "leaf_overall",
        "leaf_match_records",
        "leaf_matrix"
      ],
      "not": {
        "anyOf": [
          {"required": ["archetype_order"]},
          {"required": ["overall"]},
          {"required": ["matrix"]}
        ]
      }
    }
  ],
  "additionalProperties": false,
  "$defs": {
    "identity": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*(?:/[a-z0-9]+(?:-[a-z0-9]+)*)?$"
    },
    "identityOrder": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/identity"
      },
      "uniqueItems": true
    },
    "legacyName": {
      "type": "string",
      "minLength": 1
    },
    "legacyOrder": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/legacyName"
      },
      "uniqueItems": true
    },
    "legacyRecordMap": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/legacyName"
      },
      "additionalProperties": {
        "$ref": "#/$defs/compatibilityRecord"
      }
    },
    "legacyRecordMatrix": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/legacyName"
      },
      "additionalProperties": {
        "type": "object",
        "propertyNames": {
          "$ref": "#/$defs/legacyName"
        },
        "additionalProperties": {
          "$ref": "#/$defs/compatibilityRecord"
        }
      }
    },
    "recordMap": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/identity"
      },
      "additionalProperties": {
        "$ref": "#/$defs/compatibilityRecord"
      }
    },
    "recordMatrix": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/identity"
      },
      "additionalProperties": {
        "type": "object",
        "propertyNames": {
          "$ref": "#/$defs/identity"
        },
        "additionalProperties": {
          "$ref": "#/$defs/compatibilityRecord"
        }
      }
    },
    "compatibilityRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "wins",
        "losses",
        "draws",
        "matches",
        "win_rate",
        "ci_half",
        "low_sample",
        "mirror",
        "literal_record"
      ],
      "properties": {
        "wins": {"type": "integer", "minimum": 0},
        "losses": {"type": "integer", "minimum": 0},
        "draws": {"type": "integer", "minimum": 0},
        "matches": {"type": "integer", "minimum": 0},
        "win_rate": {"type": ["number", "null"], "minimum": 0, "maximum": 1},
        "ci_half": {"type": ["number", "null"], "minimum": 0, "maximum": 1},
        "low_sample": {"type": "boolean"},
        "mirror": {"type": "boolean"},
        "literal_record": {
          "$ref": "phase8-public-contract.schema.json#/$defs/literalRecord"
        }
      }
    },
    "matchRecordMap": {
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/identity"
      },
      "additionalProperties": {
        "$ref": "phase8-public-contract.schema.json#/$defs/matchRecord"
      }
    },
    "hierarchy": {
      "type": "object",
      "required": ["parents", "leaves"],
      "properties": {
        "parents": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/parentNode"
          }
        },
        "leaves": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/leafNode"
          }
        }
      },
      "additionalProperties": false
    },
    "parentNode": {
      "type": "object",
      "required": ["id", "name", "expandable", "subtype_ids"],
      "properties": {
        "id": {
          "$ref": "#/$defs/identity"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "expandable": {
          "type": "boolean"
        },
        "subtype_ids": {
          "$ref": "#/$defs/identityOrder"
        }
      },
      "additionalProperties": false
    },
    "leafNode": {
      "type": "object",
      "required": [
        "id",
        "kind",
        "name",
        "display_name",
        "parent_id",
        "subtype_id"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/identity"
        },
        "kind": {
          "enum": ["archetype", "subtype"]
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "display_name": {
          "type": "string",
          "minLength": 1
        },
        "parent_id": {
          "$ref": "#/$defs/identity"
        },
        "subtype_id": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "type": "string",
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
            }
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
