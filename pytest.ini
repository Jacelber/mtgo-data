[pytest]
minversion = 9.1
addopts = -ra --strict-config --strict-markers -p no:cacheprovider
testpaths = tests
python_files = test_*.py
markers =
    committed_baseline: requires a clean checkout of the committed production data baseline
